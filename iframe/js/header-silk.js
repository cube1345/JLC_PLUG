// Auto-generated from iframe/js/header-silk/*.js. Do not edit this file directly.
"use strict";
(() => {
  // iframe/js/header-silk/shared.js
  var STORAGE_KEY = "header_silk_settings_v2";
  var ARTIFACT_STORAGE_KEY = "header_silk_artifacts_v1";
  var MANUAL_LABEL_OVERRIDE_STORAGE_KEY = "header_silk_manual_labels_v1";
  var PCB_LAYER_TOP = 1;
  var PCB_LAYER_BOTTOM = 2;
  var PCB_LAYER_TOP_SILK = 3;
  var PCB_LAYER_BOTTOM_SILK = 4;
  var TEXT_RENDER_FONT_PX = 192;
  var TEXT_RENDER_FONT_WEIGHT = 400;
  var TEXT_RENDER_PADDING_X_PX = 12;
  var TEXT_RENDER_PADDING_Y_PX = 10;
  var RANGE_SELECTION_EVENT_ID = "header-silk-range-select";
  var MIN_RANGE_SELECTION_DISTANCE = 5;
  var AUTO_SHRINK_GAP_RATIO_MAJOR = 0.88;
  var AUTO_SHRINK_GAP_RATIO_MINOR = 0.82;
  var MIN_AUTO_SHRINK_SCALE = 0.68;
  var AUTO_SHRINK_EASE = 0.72;
  var PREVIEW_BASE_IMAGE_HEIGHT_PX = 28;
  var MAX_STORED_ARTIFACT_GROUPS = 80;
  var MAX_HEADER_PARSE_CACHE = 24;
  var MAX_HEADER_GEOMETRY_CACHE = 24;
  var MAX_TEXT_ASSET_CACHE = 160;
  var MAX_STORED_LABEL_OVERRIDE_COMPONENTS = 120;
  var LABEL_PREVIEW_SELECTION_POLL_MS = 700;
  var CREATE_BATCH_SIZE = 8;
  var DEFAULT_SETTINGS2 = {
    fontFamily: "黑体",
    unitMode: "mil",
    layerMode: "auto",
    fontSizeMil: 40,
    strokeWidthMil: 4,
    positionMode: "auto",
    rotationMode: "auto",
    offsetMil: 18,
    includeShell: false,
    invert: false,
    labelMapText: ""
  };
  var elements2 = {
    panelSubtitle: document.getElementById("panel-subtitle"),
    tabs: Array.from(document.querySelectorAll("[data-tab-target]")),
    tabPanels: Array.from(document.querySelectorAll("[data-tab-panel]")),
    fontFamily: document.getElementById("font-family"),
    unitMode: document.getElementById("unit-mode"),
    layerMode: document.getElementById("layer-mode"),
    fontSize: document.getElementById("font-size"),
    fontSizeUnit: document.getElementById("font-size-unit"),
    strokeWidth: document.getElementById("stroke-width"),
    strokeWidthUnit: document.getElementById("stroke-width-unit"),
    positionMode: document.getElementById("position-mode"),
    rotationMode: document.getElementById("rotation-mode"),
    offset: document.getElementById("offset"),
    offsetUnit: document.getElementById("offset-unit"),
    labelMap: document.getElementById("label-map"),
    includeShell: document.getElementById("include-shell"),
    invert: document.getElementById("invert"),
    reset: document.getElementById("reset"),
    deleteGenerated: document.getElementById("delete-generated"),
    generate: document.getElementById("generate"),
    previewCanvas: document.getElementById("preview-canvas"),
    previewSummary: document.getElementById("preview-summary"),
    refreshLabelPreview: document.getElementById("refresh-label-preview"),
    clearLabelOverrides: document.getElementById("clear-label-overrides"),
    labelPreviewMeta: document.getElementById("label-preview-meta"),
    labelPreviewList: document.getElementById("label-preview-list")
  };
  var appState = {
    currentSettings: loadSettings(),
    labelPreviewRefreshTimer: void 0,
    labelPreviewRequestId: 0,
    lastLabelPreviewSelectionSignature: "",
    labelPreviewSelectionWatcher: void 0
  };
  var headerParseCache = /* @__PURE__ */ new Map();
  var headerGeometryCache = /* @__PURE__ */ new Map();
  var textAssetCache = /* @__PURE__ */ new Map();
  var manualLabelOverrideCache = loadManualLabelOverrideCache();
  function asArray(value) {
    if (Array.isArray(value)) {
      return value;
    }
    return value == null ? [] : [value];
  }
  function normalizeText(text) {
    return String(text || "").replace(/\s+/g, " ").trim();
  }
  function clamp(value, min, max) {
    return Math.min(Math.max(value, min), max);
  }
  function milToMm(value) {
    return value * 0.0254;
  }
  function mmToMil(value) {
    return value / 0.0254;
  }
  function formatNumeric(value) {
    const rounded = Math.round(value * 1e3) / 1e3;
    return Number.isInteger(rounded) ? String(rounded) : String(rounded);
  }
  function touchMapEntry(map, key, value) {
    if (map.has(key)) {
      map.delete(key);
    }
    map.set(key, value);
  }
  function trimMapSize(map, maxSize, protectedKey) {
    while (map.size > maxSize) {
      const oldestKey = map.keys().next().value;
      if (oldestKey == null) {
        return;
      }
      if (oldestKey === protectedKey && map.size > 1) {
        const protectedValue = map.get(oldestKey);
        map.delete(oldestKey);
        map.set(oldestKey, protectedValue);
        continue;
      }
      map.delete(oldestKey);
    }
  }
  function toDisplayValue(valueMil, unitMode) {
    return unitMode === "mm" ? milToMm(valueMil) : valueMil;
  }
  function fromDisplayValue(value, unitMode) {
    return unitMode === "mm" ? mmToMil(value) : value;
  }
  function dot(a, b) {
    return a.x * b.x + a.y * b.y;
  }
  function normalizeVector(vector) {
    const length = Math.hypot(vector.x, vector.y);
    if (length <= 1e-9) {
      return { x: 1, y: 0 };
    }
    return {
      x: vector.x / length,
      y: vector.y / length
    };
  }
  function getAxis(points) {
    const center = points.reduce((acc, point) => ({
      x: acc.x + point.x,
      y: acc.y + point.y
    }), { x: 0, y: 0 });
    center.x /= points.length || 1;
    center.y /= points.length || 1;
    let sxx = 0;
    let syy = 0;
    let sxy = 0;
    for (const point of points) {
      const dx = point.x - center.x;
      const dy = point.y - center.y;
      sxx += dx * dx;
      syy += dy * dy;
      sxy += dx * dy;
    }
    const angle = 0.5 * Math.atan2(2 * sxy, sxx - syy);
    let major = normalizeVector({ x: Math.cos(angle), y: Math.sin(angle) });
    if (Math.abs(major.x) >= Math.abs(major.y)) {
      if (major.x < 0) {
        major = { x: -major.x, y: -major.y };
      }
    } else if (major.y < 0) {
      major = { x: -major.x, y: -major.y };
    }
    let minor = normalizeVector({ x: -major.y, y: major.x });
    if (Math.abs(minor.y) >= Math.abs(minor.x)) {
      if (minor.y < 0) {
        minor = { x: -minor.x, y: -minor.y };
      }
    } else if (minor.x < 0) {
      minor = { x: -minor.x, y: -minor.y };
    }
    return { center, major, minor };
  }
  function median(values) {
    if (!values.length) {
      return 0;
    }
    const sorted = [...values].sort((a, b) => a - b);
    const middle = Math.floor(sorted.length / 2);
    if (sorted.length % 2 === 0) {
      return (sorted[middle - 1] + sorted[middle]) / 2;
    }
    return sorted[middle];
  }
  function getSortedUnique(values, tolerance) {
    const sorted = [...values].sort((a, b) => a - b);
    const unique = [];
    for (const value of sorted) {
      if (!unique.length || Math.abs(value - unique[unique.length - 1]) > tolerance) {
        unique.push(value);
      }
    }
    return unique;
  }
  function getPadExtent(padShape) {
    if (!Array.isArray(padShape)) {
      return 0;
    }
    const numericValues = padShape.filter((item) => typeof item === "number" && Number.isFinite(item));
    if (!numericValues.length) {
      return 0;
    }
    return Math.max.apply(null, numericValues);
  }
  function estimatePitch(projections) {
    const unique = getSortedUnique(projections, 1e-6);
    if (unique.length < 2) {
      return 0;
    }
    const diffs = [];
    for (let index = 1; index < unique.length; index += 1) {
      const diff = unique[index] - unique[index - 1];
      if (diff > 1e-6) {
        diffs.push(diff);
      }
    }
    return median(diffs);
  }
  function clusterRows(pads, tolerance) {
    const sortedPads = [...pads].sort((a, b) => a.minorProjection - b.minorProjection);
    const rows = [];
    for (const pad of sortedPads) {
      const currentRow = rows[rows.length - 1];
      if (!currentRow) {
        rows.push({ index: 0, meanMinor: pad.minorProjection, pads: [pad] });
        continue;
      }
      if (Math.abs(pad.minorProjection - currentRow.meanMinor) <= tolerance) {
        currentRow.pads.push(pad);
        currentRow.meanMinor = currentRow.pads.reduce((sum, item) => sum + item.minorProjection, 0) / currentRow.pads.length;
      } else {
        rows.push({ index: rows.length, meanMinor: pad.minorProjection, pads: [pad] });
      }
    }
    for (const row of rows) {
      row.pads.sort((a, b) => a.majorProjection - b.majorProjection);
      for (const pad of row.pads) {
        pad.rowIndex = row.index;
      }
    }
    return rows;
  }
  function getTextRotation(axis) {
    return Math.abs(axis.major.x) >= Math.abs(axis.major.y) ? 0 : 90;
  }
  function getPadHalfSpan(padShape) {
    return getPadExtent(padShape) / 2;
  }
  function projectPoint(axis, majorValue, minorValue) {
    return {
      x: axis.center.x + axis.major.x * majorValue + axis.minor.x * minorValue,
      y: axis.center.y + axis.major.y * majorValue + axis.minor.y * minorValue
    };
  }
  function estimateShellBounds(pads, rows, nominalPitch, padExtent) {
    const majorPadMin = Math.min(...pads.map((pad) => pad.majorProjection - getPadHalfSpan(pad.padShape)));
    const majorPadMax = Math.max(...pads.map((pad) => pad.majorProjection + getPadHalfSpan(pad.padShape)));
    const minorPadMin = Math.min(...pads.map((pad) => pad.minorProjection - getPadHalfSpan(pad.padShape)));
    const minorPadMax = Math.max(...pads.map((pad) => pad.minorProjection + getPadHalfSpan(pad.padShape)));
    const majorCenterMin = Math.min(...pads.map((pad) => pad.majorProjection));
    const majorCenterMax = Math.max(...pads.map((pad) => pad.majorProjection));
    const basePitch = nominalPitch || padExtent || 40;
    const basePad = padExtent || basePitch;
    const endMargin = clamp(Math.max(basePitch * 0.14, basePad * 0.1, 4), 4, 14);
    const endClearance = clamp(Math.max(basePad * 0.08, 2), 2, 8);
    const majorMin = Math.min(majorCenterMin - endMargin, majorPadMin - endClearance);
    const majorMax = Math.max(majorCenterMax + endMargin, majorPadMax + endClearance);
    return {
      majorMin,
      majorMax,
      minorMin: minorPadMin,
      minorMax: minorPadMax
    };
  }
  function makeSilkLabel(netName, padNumber) {
    const normalizedNet = normalizeText(netName);
    if (!normalizedNet) {
      return `P${padNumber || "?"}`;
    }
    const slashSegments = normalizedNet.split("/").filter(Boolean);
    const tail = slashSegments.length ? slashSegments[slashSegments.length - 1] : normalizedNet;
    const dotSegments = tail.split(".").filter(Boolean);
    const compact = normalizeText(dotSegments.length ? dotSegments[dotSegments.length - 1] : tail);
    return compact || normalizedNet;
  }
  function escapeRegExp(text) {
    return String(text || "").replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
  }
  function wildcardToRegExpSource(pattern) {
    let source = "";
    for (const character of String(pattern || "")) {
      if (character === "*") {
        source += "(.*)";
        continue;
      }
      if (character === "?") {
        source += "(.)";
        continue;
      }
      source += escapeRegExp(character);
    }
    return source;
  }
  function sanitizeRegexFlags(flags) {
    const normalizedFlags = String(flags || "").replace(/[gy]/g, "");
    return [...new Set(normalizedFlags.split(""))].join("");
  }
  function buildPatternMappingRule(source, target) {
    const normalizedSource = normalizeText(source);
    if (!normalizedSource) {
      return void 0;
    }
    const regexLiteralMatch = normalizedSource.match(/^\/(.+)\/([dgimsuvy]*)$/);
    if (regexLiteralMatch) {
      try {
        return {
          regex: new RegExp(regexLiteralMatch[1], sanitizeRegexFlags(regexLiteralMatch[2])),
          target
        };
      } catch (e) {
        return void 0;
      }
    }
    if (normalizedSource.startsWith("re:")) {
      try {
        return {
          regex: new RegExp(`^(?:${normalizedSource.slice(3).trim()})$`, "i"),
          target
        };
      } catch (e) {
        return void 0;
      }
    }
    if (normalizedSource.includes("*") || normalizedSource.includes("?")) {
      return {
        regex: new RegExp(`^${wildcardToRegExpSource(normalizedSource)}$`, "i"),
        target
      };
    }
    return void 0;
  }
  function parseLabelMappings(labelMapText) {
    const mappingText = String(labelMapText || "");
    const exactRules = /* @__PURE__ */ new Map();
    const patternRules = [];
    let size = 0;
    for (const rawLine of mappingText.split(/\r?\n+/)) {
      const line = rawLine.trim();
      if (!line || line.startsWith("#") || line.startsWith("//")) {
        continue;
      }
      const segments = line.split(/\s*(=>|->|=|:)\s*/);
      if (!segments || segments.length < 3) {
        continue;
      }
      const source = normalizeText(segments[0]);
      const target = normalizeText(segments.slice(2).join(""));
      if (!source || !target) {
        continue;
      }
      const patternRule = buildPatternMappingRule(source, target);
      if (patternRule) {
        patternRules.push(patternRule);
        size += 1;
        continue;
      }
      exactRules.set(source.toUpperCase(), target);
      size += 1;
    }
    return {
      exactRules,
      patternRules,
      size
    };
  }
  function applyLabelMappingWithMap(label, mapping) {
    const normalizedLabel = normalizeText(label);
    if (!normalizedLabel) {
      return normalizedLabel;
    }
    const normalizedUpperCaseLabel = normalizedLabel.toUpperCase();
    if (mapping.exactRules.has(normalizedUpperCaseLabel)) {
      return mapping.exactRules.get(normalizedUpperCaseLabel) || normalizedLabel;
    }
    for (const rule of mapping.patternRules) {
      rule.regex.lastIndex = 0;
      if (!rule.regex.test(normalizedLabel)) {
        continue;
      }
      rule.regex.lastIndex = 0;
      const replacedLabel = normalizedLabel.replace(rule.regex, rule.target);
      return normalizeText(replacedLabel) || normalizedLabel;
    }
    return normalizedLabel;
  }
  function getHeaderManualLabelOverrides(componentId, createIfMissing) {
    const normalizedComponentId = normalizeText(componentId);
    if (!normalizedComponentId) {
      return void 0;
    }
    if (!manualLabelOverrideCache.has(normalizedComponentId) && createIfMissing) {
      touchMapEntry(manualLabelOverrideCache, normalizedComponentId, /* @__PURE__ */ new Map());
      trimMapSize(manualLabelOverrideCache, MAX_STORED_LABEL_OVERRIDE_COMPONENTS, normalizedComponentId);
      saveManualLabelOverrideCache(manualLabelOverrideCache);
    }
    const overrideMap = manualLabelOverrideCache.get(normalizedComponentId);
    if (overrideMap) {
      touchMapEntry(manualLabelOverrideCache, normalizedComponentId, overrideMap);
    }
    return overrideMap;
  }
  function createHeaderPadOverrideKey(pad) {
    return normalizeText(pad && pad.cachePadKey) || `${normalizeText(pad && pad.primitiveId)}:${normalizeText(pad && pad.padNumber)}`;
  }
  function getManualLabelOverrideByKey(componentId, padKey) {
    const overrideMap = getHeaderManualLabelOverrides(componentId, false);
    if (!overrideMap) {
      return "";
    }
    return normalizeText(overrideMap.get(normalizeText(padKey)));
  }
  function setManualLabelOverrideByKey(componentId, padKey, nextLabel, autoLabel) {
    const normalizedComponentId = normalizeText(componentId);
    const normalizedPadKey = normalizeText(padKey);
    if (!normalizedComponentId || !normalizedPadKey) {
      return "";
    }
    const normalizedLabel = normalizeText(nextLabel);
    const normalizedAutoLabel = normalizeText(autoLabel);
    const overrideMap = getHeaderManualLabelOverrides(normalizedComponentId, Boolean(normalizedLabel));
    if (!overrideMap) {
      return "";
    }
    if (!normalizedLabel || normalizedLabel === normalizedAutoLabel) {
      overrideMap.delete(normalizedPadKey);
      if (!overrideMap.size) {
        manualLabelOverrideCache.delete(normalizedComponentId);
      }
      saveManualLabelOverrideCache(manualLabelOverrideCache);
      return "";
    }
    overrideMap.set(normalizedPadKey, normalizedLabel);
    touchMapEntry(manualLabelOverrideCache, normalizedComponentId, overrideMap);
    trimMapSize(manualLabelOverrideCache, MAX_STORED_LABEL_OVERRIDE_COMPONENTS, normalizedComponentId);
    saveManualLabelOverrideCache(manualLabelOverrideCache);
    return normalizedLabel;
  }
  function clearManualLabelOverrides(componentId) {
    const normalizedComponentId = normalizeText(componentId);
    if (!normalizedComponentId || !manualLabelOverrideCache.has(normalizedComponentId)) {
      return false;
    }
    manualLabelOverrideCache.delete(normalizedComponentId);
    saveManualLabelOverrideCache(manualLabelOverrideCache);
    return true;
  }
  function resolveHeaderPadDisplayLabel(header, pad, labelMappings) {
    const autoLabel = applyLabelMappingWithMap(makeSilkLabel(pad.netName, pad.padNumber), labelMappings);
    const manualLabel = getManualLabelOverrideByKey(header && header.componentId, createHeaderPadOverrideKey(pad));
    return {
      autoLabel,
      manualLabel,
      displayLabel: manualLabel || autoLabel,
      hasManualOverride: Boolean(manualLabel)
    };
  }
  function getComponentDisplayName(component) {
    const designator = normalizeText(component.getState_Designator && component.getState_Designator());
    if (designator) {
      return designator;
    }
    const name = normalizeText(component.getState_Name && component.getState_Name());
    if (name) {
      return name;
    }
    return normalizeText(component.getState_PrimitiveId && component.getState_PrimitiveId()) || "HEADER";
  }
  function comparePadNumbers(leftPadNumber, rightPadNumber) {
    return String(leftPadNumber || "").localeCompare(String(rightPadNumber || ""), "en", {
      numeric: true,
      sensitivity: "base"
    });
  }
  function getHeaderDisplayLabelItems(header, settings) {
    const labelMappings = parseLabelMappings(settings && settings.labelMapText);
    return header.rows.flatMap(
      (row) => [...row.pads].sort((leftPad, rightPad) => {
        const padNumberOrder = comparePadNumbers(leftPad.padNumber, rightPad.padNumber);
        if (padNumberOrder !== 0) {
          return padNumberOrder;
        }
        return leftPad.majorProjection - rightPad.majorProjection;
      }).map((pad) => ({
        rowIndex: row.index,
        componentId: header.componentId,
        padKey: createHeaderPadOverrideKey(pad),
        padNumber: pad.padNumber,
        netName: pad.netName,
        ...resolveHeaderPadDisplayLabel(header, pad, labelMappings)
      }))
    );
  }
  function loadSettings() {
    try {
      const saved = JSON.parse(localStorage.getItem(STORAGE_KEY) || "null");
      return { ...DEFAULT_SETTINGS2, ...saved || {} };
    } catch (e) {
      return { ...DEFAULT_SETTINGS2 };
    }
  }
  function saveSettings(settings) {
    localStorage.setItem(STORAGE_KEY, JSON.stringify(settings));
  }
  function loadArtifactGroups() {
    try {
      const saved = JSON.parse(localStorage.getItem(ARTIFACT_STORAGE_KEY) || "[]");
      return Array.isArray(saved) ? saved.filter((item) => item && typeof item === "object") : [];
    } catch (e) {
      return [];
    }
  }
  function saveArtifactGroups(groups) {
    localStorage.setItem(
      ARTIFACT_STORAGE_KEY,
      JSON.stringify((Array.isArray(groups) ? groups : []).slice(-MAX_STORED_ARTIFACT_GROUPS))
    );
  }
  function loadManualLabelOverrideCache() {
    try {
      const saved = JSON.parse(localStorage.getItem(MANUAL_LABEL_OVERRIDE_STORAGE_KEY) || "[]");
      const cache = /* @__PURE__ */ new Map();
      for (const entry of asArray(saved)) {
        const componentId = normalizeText(entry && entry.componentId);
        if (!componentId) {
          continue;
        }
        const labels = /* @__PURE__ */ new Map();
        for (const item of asArray(entry && entry.labels)) {
          const padKey = normalizeText(item && item.padKey);
          const label = normalizeText(item && item.label);
          if (!padKey || !label) {
            continue;
          }
          labels.set(padKey, label);
        }
        if (labels.size) {
          cache.set(componentId, labels);
        }
      }
      trimMapSize(cache, MAX_STORED_LABEL_OVERRIDE_COMPONENTS);
      return cache;
    } catch (e) {
      return /* @__PURE__ */ new Map();
    }
  }
  function saveManualLabelOverrideCache(cache) {
    const serialized = [];
    for (const [componentId, labels] of cache.entries()) {
      if (!componentId || !(labels instanceof Map) || !labels.size) {
        continue;
      }
      serialized.push({
        componentId,
        labels: [...labels.entries()].map(([padKey, label]) => ({ padKey, label }))
      });
    }
    localStorage.setItem(
      MANUAL_LABEL_OVERRIDE_STORAGE_KEY,
      JSON.stringify(serialized.slice(-MAX_STORED_LABEL_OVERRIDE_COMPONENTS))
    );
  }
  function createArtifactGroupId() {
    return `${Date.now().toString(36)}-${Math.random().toString(36).slice(2, 8)}`;
  }
  function createComponentStateSignature(componentId, componentLayer, componentX, componentY, componentRotation) {
    return [
      normalizeText(componentId),
      String(Number(componentLayer) || 0),
      String(Math.round((Number(componentX) || 0) * 1e3) / 1e3),
      String(Math.round((Number(componentY) || 0) * 1e3) / 1e3),
      String(Math.round((Number(componentRotation) || 0) * 1e3) / 1e3)
    ].join("::");
  }
  function createPadStateSignature(componentPadStates, options) {
    const includeNet = Boolean(options && options.includeNet);
    return asArray(componentPadStates).map((item) => ({
      primitiveId: normalizeText(item && item.primitiveId),
      padNumber: normalizeText(item && item.padNumber),
      net: includeNet ? normalizeText(item && item.net) : ""
    })).sort((leftPad, rightPad) => {
      const leftKey = `${leftPad.primitiveId}:${leftPad.padNumber}`;
      const rightKey = `${rightPad.primitiveId}:${rightPad.padNumber}`;
      return leftKey.localeCompare(rightKey, "en", {
        numeric: true,
        sensitivity: "base"
      });
    }).map((item) => includeNet ? `${item.primitiveId}:${item.padNumber}:${item.net}` : `${item.primitiveId}:${item.padNumber}`).join("|");
  }
  function createHeaderGeometryCacheKey(componentId, componentLayer, componentX, componentY, componentRotation, componentPadStates) {
    return [
      createComponentStateSignature(componentId, componentLayer, componentX, componentY, componentRotation),
      createPadStateSignature(componentPadStates, { includeNet: false })
    ].join("::");
  }
  function createHeaderParseCacheKey(componentId, componentLayer, componentX, componentY, componentRotation, componentPadStates) {
    return [
      createComponentStateSignature(componentId, componentLayer, componentX, componentY, componentRotation),
      createPadStateSignature(componentPadStates, { includeNet: true })
    ].join("::");
  }
  function rememberBoundedCache(cache, cacheKey, value, maxSize) {
    if (!cacheKey || !value) {
      return;
    }
    if (cache.has(cacheKey)) {
      cache.delete(cacheKey);
    }
    cache.set(cacheKey, value);
    while (cache.size > maxSize) {
      const oldestKey = cache.keys().next().value;
      if (!oldestKey) {
        break;
      }
      cache.delete(oldestKey);
    }
  }
  async function getCurrentDocumentScope() {
    try {
      const currentDocumentInfo = await eda.dmt_SelectControl.getCurrentDocumentInfo();
      return {
        documentUuid: normalizeText(currentDocumentInfo && currentDocumentInfo.uuid),
        parentProjectUuid: normalizeText(currentDocumentInfo && currentDocumentInfo.parentProjectUuid)
      };
    } catch (e) {
      return {
        documentUuid: "",
        parentProjectUuid: ""
      };
    }
  }
  function doesArtifactGroupMatchHeader(group, scope, header) {
    if (!group || typeof group !== "object") {
      return false;
    }
    if (normalizeText(group.headerComponentId) !== normalizeText(header.componentId)) {
      return false;
    }
    const groupDocumentUuid = normalizeText(group.documentUuid);
    const groupProjectUuid = normalizeText(group.parentProjectUuid);
    if (scope.documentUuid && groupDocumentUuid && groupDocumentUuid !== scope.documentUuid) {
      return false;
    }
    if (scope.parentProjectUuid && groupProjectUuid && groupProjectUuid !== scope.parentProjectUuid) {
      return false;
    }
    return true;
  }
  function collectPrimitiveIdsFromHandles(handles) {
    return handles.reduce((accumulator, handle) => {
      const primitiveId = handle && handle.primitive && handle.primitive.getState_PrimitiveId && handle.primitive.getState_PrimitiveId();
      if (!primitiveId) {
        return accumulator;
      }
      if (handle.type === "image") {
        accumulator.imagePrimitiveIds.push(String(primitiveId));
      } else if (handle.type === "line") {
        accumulator.linePrimitiveIds.push(String(primitiveId));
      }
      return accumulator;
    }, {
      imagePrimitiveIds: [],
      linePrimitiveIds: []
    });
  }
  async function rememberGeneratedArtifacts(header, layer, handles) {
    const primitiveIds = collectPrimitiveIdsFromHandles(handles);
    if (!primitiveIds.imagePrimitiveIds.length && !primitiveIds.linePrimitiveIds.length) {
      return;
    }
    const scope = await getCurrentDocumentScope();
    const groups = loadArtifactGroups();
    groups.push({
      id: createArtifactGroupId(),
      documentUuid: scope.documentUuid,
      parentProjectUuid: scope.parentProjectUuid,
      headerComponentId: normalizeText(header.componentId),
      designator: normalizeText(header.designator),
      layer: Number(layer) || 0,
      imagePrimitiveIds: primitiveIds.imagePrimitiveIds,
      linePrimitiveIds: primitiveIds.linePrimitiveIds,
      createdAt: (/* @__PURE__ */ new Date()).toISOString()
    });
    saveArtifactGroups(groups);
  }
  async function resolveExistingImagesByIds(primitiveIds) {
    const normalizedIds = [...new Set(asArray(primitiveIds).map((item) => normalizeText(item)).filter(Boolean))];
    if (!normalizedIds.length) {
      return [];
    }
    return asArray(await eda.pcb_PrimitiveImage.get(normalizedIds));
  }
  async function resolveExistingLinesByIds(primitiveIds) {
    const normalizedIds = [...new Set(asArray(primitiveIds).map((item) => normalizeText(item)).filter(Boolean))];
    if (!normalizedIds.length) {
      return [];
    }
    return asArray(await eda.pcb_PrimitiveLine.get(normalizedIds));
  }
  async function deleteArtifactGroupByRecord(group) {
    const images = await resolveExistingImagesByIds(group && group.imagePrimitiveIds);
    const lines = await resolveExistingLinesByIds(group && group.linePrimitiveIds);
    if (images.length) {
      await eda.pcb_PrimitiveImage.delete(images);
    }
    if (lines.length) {
      await eda.pcb_PrimitiveLine.delete(lines);
    }
    return images.length + lines.length;
  }

  // iframe/js/header-silk/rendering.js
  function drawRoundedRect(context, x, y, width, height, radius) {
    context.beginPath();
    context.moveTo(x + radius, y);
    context.arcTo(x + width, y, x + width, y + height, radius);
    context.arcTo(x + width, y + height, x, y + height, radius);
    context.arcTo(x, y + height, x, y, radius);
    context.arcTo(x, y, x + width, y, radius);
    context.closePath();
  }
  function drawPreviewCrosshair(context, centerX, centerY) {
    context.save();
    context.strokeStyle = "rgba(158, 79, 29, 0.5)";
    context.lineWidth = 1;
    context.setLineDash([4, 4]);
    context.beginPath();
    context.moveTo(centerX - 44, centerY);
    context.lineTo(centerX + 44, centerY);
    context.moveTo(centerX, centerY - 44);
    context.lineTo(centerX, centerY + 44);
    context.stroke();
    context.setLineDash([]);
    context.fillStyle = "#9e4f1d";
    context.beginPath();
    context.arc(centerX, centerY, 3, 0, Math.PI * 2);
    context.fill();
    context.font = '12px "Microsoft YaHei", sans-serif';
    context.fillStyle = "rgba(107, 90, 72, 0.95)";
    context.fillText("落点", centerX + 10, centerY - 10);
    context.restore();
  }
  function getPreviewRotation(settings, orientation) {
    if (settings.rotationMode === "auto") {
      return orientation === "vertical" ? 90 : 0;
    }
    return Number(settings.rotationMode) || 0;
  }
  function getTextRenderSettings(settings) {
    const fontSizeMil = Math.max(Number(settings && settings.fontSizeMil) || 1, 1);
    const strokeWidthMil = clamp(Number(settings && settings.strokeWidthMil) || 0, 0, fontSizeMil * 0.38);
    const marginXMil = clamp(fontSizeMil * 0.05 + strokeWidthMil * 0.45, 1.6, 5.5);
    const marginYMil = clamp(fontSizeMil * 0.075 + strokeWidthMil * 0.55, 2, 7);
    return {
      fontSizeMil,
      strokeWidthMil,
      marginXMil,
      marginYMil
    };
  }
  function getPreviewImageHeightPx(settings) {
    const { fontSizeMil } = getTextRenderSettings(settings);
    const scale = clamp(fontSizeMil / DEFAULT_SETTINGS2.fontSizeMil, 0.45, 2.4);
    return clamp(PREVIEW_BASE_IMAGE_HEIGHT_PX * scale, 14, 72);
  }
  function drawPreviewLabel(context, text, centerX, centerY, rotation, settings, previewImageHeightPx, metrics) {
    const pixelScale = previewImageHeightPx / Math.max(metrics.canvasHeightPx, 1);
    const fontPx = Math.max(TEXT_RENDER_FONT_PX * pixelScale, 9);
    const strokePx = metrics.strokeWidthPx <= 0 ? 0 : Math.max(metrics.strokeWidthPx * pixelScale, 0.5);
    context.save();
    context.translate(centerX, centerY);
    context.rotate(rotation * Math.PI / 180);
    context.textAlign = "center";
    context.textBaseline = "middle";
    context.font = `${TEXT_RENDER_FONT_WEIGHT} ${fontPx}px "${settings.fontFamily}", sans-serif`;
    context.lineJoin = "round";
    context.lineCap = "round";
    context.lineWidth = strokePx;
    const textWidth = Math.max(context.measureText(text).width, fontPx * 1.2);
    const paddingX = Math.max(metrics.paddingXPx * pixelScale - strokePx, 3) + strokePx;
    const paddingY = Math.max(metrics.paddingYPx * pixelScale - strokePx, 2.5) + strokePx;
    const boxWidth = textWidth + paddingX * 2;
    const boxHeight = fontPx + paddingY * 2;
    if (settings.invert) {
      drawRoundedRect(context, -boxWidth / 2, -boxHeight / 2, boxWidth, boxHeight, 10);
      context.fillStyle = "#3a2b1b";
      context.fill();
      context.fillStyle = "#fff8ef";
      context.strokeStyle = "#fff8ef";
    } else {
      context.fillStyle = "#2a2015";
      context.strokeStyle = "#2a2015";
    }
    if (strokePx > 0) {
      context.strokeText(text, 0, 1);
    }
    context.fillText(text, 0, 1);
    context.restore();
  }
  function renderPreview() {
    const canvas = elements.previewCanvas;
    const summary = elements.previewSummary;
    if (!canvas || !summary) {
      return;
    }
    const context = canvas.getContext("2d");
    if (!context) {
      return;
    }
    const width = canvas.width;
    const height = canvas.height;
    const centerX = width / 2;
    const centerY = height / 2;
    const labelMappings = parseLabelMappings(appState.currentSettings.labelMapText);
    const labels = ["3V3", "SCL", "TX", "RX", "GND"].map((label) => applyLabelMappingWithMap(label, labelMappings));
    const orientation = appState.currentSettings.positionMode === "left" || appState.currentSettings.positionMode === "right" ? "vertical" : "horizontal";
    const previewRotation = getPreviewRotation(appState.currentSettings, orientation);
    const textMetrics = getTextSourceMetrics(appState.currentSettings);
    const previewImageHeightPx = getPreviewImageHeightPx(appState.currentSettings);
    const previewStrokePx = textMetrics.strokeWidthPx <= 0 ? 0 : Math.max(previewImageHeightPx / Math.max(textMetrics.canvasHeightPx, 1) * textMetrics.strokeWidthPx, 0.5);
    const offsetPx = clamp(appState.currentSettings.offsetMil * 0.55, 14, 56);
    context.clearRect(0, 0, width, height);
    drawPreviewCrosshair(context, centerX, centerY);
    context.save();
    context.fillStyle = "rgba(73, 57, 38, 0.08)";
    context.strokeStyle = "rgba(73, 57, 38, 0.18)";
    context.lineWidth = 1.2;
    const bodyWidth = orientation === "horizontal" ? 208 : 38;
    const bodyHeight = orientation === "horizontal" ? 36 : 152;
    drawRoundedRect(context, centerX - bodyWidth / 2, centerY - bodyHeight / 2, bodyWidth, bodyHeight, 14);
    context.fill();
    context.stroke();
    context.font = '12px "Microsoft YaHei", sans-serif';
    context.fillStyle = "rgba(73, 57, 38, 0.8)";
    context.textAlign = "center";
    context.textBaseline = "middle";
    context.fillText("预览", centerX, centerY);
    context.restore();
    if (appState.currentSettings.includeShell) {
      context.save();
      context.strokeStyle = "rgba(43, 32, 21, 0.8)";
      context.lineWidth = Math.max(previewStrokePx, 1.2);
      drawRoundedRect(
        context,
        centerX - bodyWidth / 2 - 12,
        centerY - bodyHeight / 2 - 12,
        bodyWidth + 24,
        bodyHeight + 24,
        12
      );
      context.stroke();
      context.restore();
    }
    let direction = { x: 0, y: -1 };
    switch (appState.currentSettings.positionMode) {
      case "bottom":
        direction = { x: 0, y: 1 };
        break;
      case "left":
        direction = { x: -1, y: 0 };
        break;
      case "right":
        direction = { x: 1, y: 0 };
        break;
      default:
        direction = orientation === "horizontal" ? { x: 0, y: -1 } : { x: 1, y: 0 };
        break;
    }
    const padGap = orientation === "horizontal" ? 42 : 30;
    const padStart = -((labels.length - 1) * padGap) / 2;
    for (let index = 0; index < labels.length; index += 1) {
      const padX = orientation === "horizontal" ? centerX + padStart + index * padGap : centerX;
      const padY = orientation === "horizontal" ? centerY : centerY + padStart + index * padGap;
      context.save();
      context.fillStyle = "#b67643";
      context.beginPath();
      context.arc(padX, padY, 5.5, 0, Math.PI * 2);
      context.fill();
      context.restore();
      const labelX = padX + direction.x * (offsetPx + 22);
      const labelY = padY + direction.y * (offsetPx + 18);
      context.save();
      context.strokeStyle = "rgba(158, 79, 29, 0.34)";
      context.lineWidth = 1.2;
      context.beginPath();
      context.moveTo(padX, padY);
      context.lineTo(
        padX + direction.x * (offsetPx + 8),
        padY + direction.y * (offsetPx + 8)
      );
      context.stroke();
      context.restore();
      drawPreviewLabel(context, labels[index], labelX, labelY, previewRotation, appState.currentSettings, previewImageHeightPx, textMetrics);
    }
    const fontSizeDisplay = formatNumeric(toDisplayValue(appState.currentSettings.fontSizeMil, appState.currentSettings.unitMode));
    const strokeDisplay = formatNumeric(toDisplayValue(appState.currentSettings.strokeWidthMil, appState.currentSettings.unitMode));
    const offsetDisplay = formatNumeric(toDisplayValue(appState.currentSettings.offsetMil, appState.currentSettings.unitMode));
    const shellText = appState.currentSettings.includeShell ? "，含外框" : "";
    const mappingCount = labelMappings.size;
    const mappingText = mappingCount > 0 ? `，映射 ${mappingCount} 项` : "";
    const layerText = appState.currentSettings.layerMode === "top" ? "顶层" : appState.currentSettings.layerMode === "bottom" ? "底层" : "自动";
    summary.textContent = `字号 ${fontSizeDisplay} ${appState.currentSettings.unitMode}，粗细 ${strokeDisplay} ${appState.currentSettings.unitMode}，偏移 ${offsetDisplay} ${appState.currentSettings.unitMode}，${layerText}${shellText}${mappingText}，密集区域自动微缩。`;
  }
  function dataUrlToBlob(dataUrl) {
    const base64Data = dataUrl.split(",")[1] || "";
    const byteCharacters = atob(base64Data);
    const byteArray = new Uint8Array(byteCharacters.length);
    for (let index = 0; index < byteCharacters.length; index += 1) {
      byteArray[index] = byteCharacters.charCodeAt(index);
    }
    return new Blob([byteArray], { type: "image/png" });
  }
  function getImageTopLeftFromCenter(centerX, centerY, width, height, rotation) {
    const normalizedRotation = (Math.round(rotation) % 360 + 360) % 360;
    switch (normalizedRotation) {
      case 0:
        return { x: centerX - width / 2, y: centerY + height / 2 };
      case 90:
        return { x: centerX - height / 2, y: centerY - width / 2 };
      case 180:
        return { x: centerX + width / 2, y: centerY - height / 2 };
      case 270:
        return { x: centerX + height / 2, y: centerY + width / 2 };
      default:
        return { x: centerX - width / 2, y: centerY + height / 2 };
    }
  }
  function getImageCenterFromTopLeft(topLeftX, topLeftY, width, height, rotation) {
    const normalizedRotation = (Math.round(rotation) % 360 + 360) % 360;
    switch (normalizedRotation) {
      case 0:
        return { x: topLeftX + width / 2, y: topLeftY - height / 2 };
      case 90:
        return { x: topLeftX + height / 2, y: topLeftY + width / 2 };
      case 180:
        return { x: topLeftX - width / 2, y: topLeftY + height / 2 };
      case 270:
        return { x: topLeftX - height / 2, y: topLeftY - width / 2 };
      default:
        return { x: topLeftX + width / 2, y: topLeftY - height / 2 };
    }
  }
  function getTextSourceMetrics(settings) {
    const textSettings = getTextRenderSettings(settings);
    const baseHeightPx = Math.ceil(TEXT_RENDER_FONT_PX * 1.04) + TEXT_RENDER_PADDING_Y_PX * 2;
    const pxPerMil = baseHeightPx / textSettings.fontSizeMil;
    const strokeWidthPx = textSettings.strokeWidthMil * pxPerMil;
    const canvasHeightPx = baseHeightPx + strokeWidthPx * 2;
    const imageScale = textSettings.fontSizeMil / canvasHeightPx;
    return {
      strokeWidthPx,
      canvasHeightPx,
      imageScale,
      paddingXPx: TEXT_RENDER_PADDING_X_PX + strokeWidthPx,
      paddingYPx: TEXT_RENDER_PADDING_Y_PX + strokeWidthPx
    };
  }
  function getTextRenderPlan(text, settings) {
    const measureCanvas = document.createElement("canvas");
    const measureContext = measureCanvas.getContext("2d");
    if (!measureContext) {
      throw new Error("生成失败，请重试。");
    }
    const textSettings = getTextRenderSettings(settings);
    const fontDeclaration = `${TEXT_RENDER_FONT_WEIGHT} ${TEXT_RENDER_FONT_PX}px "${settings.fontFamily}", sans-serif`;
    measureContext.font = fontDeclaration;
    measureContext.textBaseline = "alphabetic";
    const measured = measureContext.measureText(text);
    const ascentPx = Math.max(measured.actualBoundingBoxAscent || TEXT_RENDER_FONT_PX * 0.78, 1);
    const descentPx = Math.max(measured.actualBoundingBoxDescent || TEXT_RENDER_FONT_PX * 0.22, 1);
    const leftPx = Math.max(measured.actualBoundingBoxLeft || 0, 0);
    const rightPx = Math.max(measured.actualBoundingBoxRight || measured.width || TEXT_RENDER_FONT_PX * 0.6, 1);
    const fillWidthPx = Math.max(leftPx + rightPx, 1);
    const fillHeightPx = Math.max(ascentPx + descentPx, 1);
    const pxPerMil = fillHeightPx / textSettings.fontSizeMil;
    const strokeWidthPx = textSettings.strokeWidthMil * pxPerMil;
    const marginXPx = textSettings.marginXMil * pxPerMil;
    const marginYPx = textSettings.marginYMil * pxPerMil;
    const paddingXPx = marginXPx + strokeWidthPx + 1;
    const paddingYPx = marginYPx + strokeWidthPx + 1;
    const canvasWidthPx = Math.ceil(fillWidthPx + paddingXPx * 2);
    const canvasHeightPx = Math.ceil(fillHeightPx + paddingYPx * 2);
    return {
      fontDeclaration,
      ascentPx,
      leftPx,
      fillWidthPx,
      fillHeightPx,
      pxPerMil,
      strokeWidthPx,
      marginXPx,
      marginYPx,
      paddingXPx,
      paddingYPx,
      canvasWidthPx,
      canvasHeightPx,
      imageWidthMil: canvasWidthPx / pxPerMil,
      imageHeightMil: canvasHeightPx / pxPerMil
    };
  }
  async function renderTextToBlob(text, settings, plan) {
    const renderPlan = plan || getTextRenderPlan(text, settings);
    const strokeWidthPx = renderPlan.strokeWidthPx;
    const canvas = document.createElement("canvas");
    const context = canvas.getContext("2d");
    if (!context) {
      throw new Error("生成失败，请重试。");
    }
    canvas.width = renderPlan.canvasWidthPx;
    canvas.height = renderPlan.canvasHeightPx;
    const drawContext = canvas.getContext("2d");
    if (!drawContext) {
      throw new Error("生成失败，请重试。");
    }
    drawContext.clearRect(0, 0, canvas.width, canvas.height);
    drawContext.lineJoin = "round";
    drawContext.lineCap = "round";
    drawContext.lineWidth = strokeWidthPx;
    drawContext.textBaseline = "alphabetic";
    drawContext.font = renderPlan.fontDeclaration;
    if (settings.invert) {
      drawContext.fillStyle = "#000000";
      drawRoundedRect(drawContext, 0, 0, canvas.width, canvas.height, Math.max(8, canvas.height * 0.18));
      drawContext.fill();
      drawContext.fillStyle = "#ffffff";
      drawContext.strokeStyle = "#ffffff";
    } else {
      drawContext.fillStyle = "#000000";
      drawContext.strokeStyle = "#000000";
    }
    const textX = renderPlan.paddingXPx + renderPlan.leftPx;
    const textY = renderPlan.paddingYPx + renderPlan.ascentPx;
    if (strokeWidthPx > 0) {
      drawContext.strokeText(text, textX, textY);
    }
    drawContext.fillText(text, textX, textY);
    return {
      blob: dataUrlToBlob(canvas.toDataURL("image/png")),
      widthPx: canvas.width,
      heightPx: canvas.height,
      imageWidthMil: renderPlan.imageWidthMil,
      imageHeightMil: renderPlan.imageHeightMil
    };
  }

  // iframe/js/header-silk/header.js
  async function getSelectedComponents() {
    if (!eda.pcb_SelectControl || !eda.pcb_PrimitiveComponent) {
      throw new Error("请在 PCB 中使用。");
    }
    const selectedPrimitives = await eda.pcb_SelectControl.getAllSelectedPrimitives();
    const componentPrimitiveIds = /* @__PURE__ */ new Set();
    for (const primitive of asArray(selectedPrimitives)) {
      const primitiveType = String(primitive.getState_PrimitiveType && primitive.getState_PrimitiveType());
      if (primitiveType === "Component") {
        const primitiveId = primitive.getState_PrimitiveId && primitive.getState_PrimitiveId();
        if (primitiveId) {
          componentPrimitiveIds.add(primitiveId);
        }
        continue;
      }
      if (primitiveType === "ComponentPad") {
        const parentComponentPrimitiveId = primitive.getState_ParentComponentPrimitiveId && primitive.getState_ParentComponentPrimitiveId();
        if (parentComponentPrimitiveId) {
          componentPrimitiveIds.add(parentComponentPrimitiveId);
        }
      }
    }
    if (!componentPrimitiveIds.size) {
      return [];
    }
    return asArray(await eda.pcb_PrimitiveComponent.get(Array.from(componentPrimitiveIds)));
  }
  function getHeaderPadCacheKey(primitiveId, padNumber) {
    const normalizedPrimitiveId = normalizeText(primitiveId);
    const normalizedPadNumber = normalizeText(padNumber);
    return normalizedPrimitiveId || `pad-number:${normalizedPadNumber}`;
  }
  function buildComponentPadStateMaps(componentPadStates) {
    const componentPadStateByPrimitiveId = /* @__PURE__ */ new Map();
    const componentPadStateByPadNumber = /* @__PURE__ */ new Map();
    for (const item of componentPadStates) {
      const primitiveId = normalizeText(item && item.primitiveId);
      const padNumber = normalizeText(item && item.padNumber);
      const net = normalizeText(item && item.net);
      if (primitiveId) {
        componentPadStateByPrimitiveId.set(primitiveId, { net, padNumber });
      }
      if (padNumber) {
        componentPadStateByPadNumber.set(padNumber, { net, primitiveId });
      }
    }
    return {
      componentPadStateByPrimitiveId,
      componentPadStateByPadNumber
    };
  }
  function createHeaderFromGeometry(geometry, designator, componentPadStates) {
    const {
      componentPadStateByPrimitiveId,
      componentPadStateByPadNumber
    } = buildComponentPadStateMaps(componentPadStates);
    const padItems = geometry.basePads.map((basePad) => {
      const stateByPrimitiveId = componentPadStateByPrimitiveId.get(basePad.primitiveId);
      const stateByPadNumber = componentPadStateByPadNumber.get(basePad.padNumber);
      const netName = normalizeText(
        stateByPrimitiveId && stateByPrimitiveId.net || stateByPadNumber && stateByPadNumber.net || ""
      );
      return {
        primitiveId: basePad.primitiveId,
        padNumber: basePad.padNumber,
        netName,
        silkLabel: makeSilkLabel(netName, basePad.padNumber),
        x: basePad.x,
        y: basePad.y,
        padShape: basePad.padShape,
        majorProjection: basePad.majorProjection,
        minorProjection: basePad.minorProjection,
        rowIndex: basePad.rowIndex,
        cachePadKey: basePad.cachePadKey
      };
    });
    const padItemByCacheKey = new Map(padItems.map((padItem) => [padItem.cachePadKey, padItem]));
    const rows = geometry.rowDefinitions.map((rowDefinition) => ({
      index: rowDefinition.index,
      meanMinor: rowDefinition.meanMinor,
      pads: rowDefinition.padKeys.map((padKey) => padItemByCacheKey.get(padKey)).filter(Boolean)
    }));
    const recognizedNetCount = padItems.filter((item) => item.netName.length > 0).length;
    return {
      componentId: geometry.componentId,
      designator,
      padCount: padItems.length,
      recognizedNetCount,
      componentLayer: geometry.componentLayer,
      defaultTextLayer: geometry.defaultTextLayer,
      textRotation: geometry.textRotation,
      nominalPitch: geometry.nominalPitch,
      padExtent: geometry.padExtent,
      axis: geometry.axis,
      shellBounds: geometry.shellBounds,
      rows,
      pads: padItems
    };
  }
  async function buildHeaderComponent(component) {
    const componentId = String(component.getState_PrimitiveId && component.getState_PrimitiveId());
    const designator = getComponentDisplayName(component);
    const componentPadStates = asArray(component.getState_Pads && component.getState_Pads());
    const componentLayer = Number(component.getState_Layer && component.getState_Layer()) || PCB_LAYER_TOP;
    const componentX = Number(component.getState_X && component.getState_X()) || 0;
    const componentY = Number(component.getState_Y && component.getState_Y()) || 0;
    const componentRotation = Number(component.getState_Rotation && component.getState_Rotation()) || 0;
    const geometryCacheKey = createHeaderGeometryCacheKey(
      componentId,
      componentLayer,
      componentX,
      componentY,
      componentRotation,
      componentPadStates
    );
    const cacheKey = createHeaderParseCacheKey(
      componentId,
      componentLayer,
      componentX,
      componentY,
      componentRotation,
      componentPadStates
    );
    const cachedHeader = headerParseCache.get(cacheKey);
    if (cachedHeader) {
      rememberBoundedCache(headerParseCache, cacheKey, cachedHeader, MAX_HEADER_PARSE_CACHE);
      return cachedHeader;
    }
    const cachedGeometry = headerGeometryCache.get(geometryCacheKey);
    if (cachedGeometry) {
      rememberBoundedCache(headerGeometryCache, geometryCacheKey, cachedGeometry, MAX_HEADER_GEOMETRY_CACHE);
      const header2 = createHeaderFromGeometry(cachedGeometry, designator, componentPadStates);
      rememberBoundedCache(headerParseCache, cacheKey, header2, MAX_HEADER_PARSE_CACHE);
      return header2;
    }
    const pads = asArray(await component.getAllPins());
    if (pads.length < 2) {
      return void 0;
    }
    const {
      componentPadStateByPrimitiveId
    } = buildComponentPadStateMaps(componentPadStates);
    const textLayer = componentLayer === PCB_LAYER_BOTTOM ? PCB_LAYER_BOTTOM_SILK : PCB_LAYER_TOP_SILK;
    const basePads = pads.map((pad) => {
      const x = Number(pad.getState_X && pad.getState_X()) || 0;
      const y = Number(pad.getState_Y && pad.getState_Y()) || 0;
      const primitiveId = normalizeText(pad.getState_PrimitiveId && pad.getState_PrimitiveId());
      const stateByPrimitiveId = componentPadStateByPrimitiveId.get(primitiveId);
      const padNumber = normalizeText(
        stateByPrimitiveId && stateByPrimitiveId.padNumber || pad.getState_PadNumber && pad.getState_PadNumber()
      );
      return {
        cachePadKey: getHeaderPadCacheKey(primitiveId, padNumber),
        primitiveId,
        padNumber,
        x,
        y,
        padShape: pad.getState_Pad && pad.getState_Pad(),
        majorProjection: 0,
        minorProjection: 0,
        rowIndex: 0
      };
    });
    const axis = getAxis(basePads.map((pad) => ({
      x: pad.x,
      y: pad.y
    })));
    for (const basePad of basePads) {
      basePad.majorProjection = dot({ x: basePad.x - axis.center.x, y: basePad.y - axis.center.y }, axis.major);
      basePad.minorProjection = dot({ x: basePad.x - axis.center.x, y: basePad.y - axis.center.y }, axis.minor);
    }
    const padExtent = median(basePads.map((item) => getPadExtent(item.padShape)).filter((size) => size > 0));
    const pitch = estimatePitch(basePads.map((item) => item.majorProjection));
    const baseSize = padExtent || pitch || 1.27;
    const rowTolerance = Math.max(baseSize * 0.6, pitch * 0.35 || 0);
    const rows = clusterRows(basePads, rowTolerance || baseSize * 0.6);
    const shellBounds = estimateShellBounds(basePads, rows, pitch || baseSize, padExtent || baseSize);
    const geometry = {
      componentId,
      componentLayer,
      defaultTextLayer: textLayer,
      textRotation: getTextRotation(axis),
      nominalPitch: pitch || baseSize,
      padExtent: padExtent || baseSize,
      axis,
      shellBounds,
      basePads: basePads.map((basePad) => ({
        cachePadKey: basePad.cachePadKey,
        primitiveId: basePad.primitiveId,
        padNumber: basePad.padNumber,
        x: basePad.x,
        y: basePad.y,
        padShape: basePad.padShape,
        majorProjection: basePad.majorProjection,
        minorProjection: basePad.minorProjection,
        rowIndex: basePad.rowIndex
      })),
      rowDefinitions: rows.map((row) => ({
        index: row.index,
        meanMinor: row.meanMinor,
        padKeys: row.pads.map((pad) => pad.cachePadKey)
      }))
    };
    rememberBoundedCache(headerGeometryCache, geometryCacheKey, geometry, MAX_HEADER_GEOMETRY_CACHE);
    const header = createHeaderFromGeometry(geometry, designator, componentPadStates);
    rememberBoundedCache(headerParseCache, cacheKey, header, MAX_HEADER_PARSE_CACHE);
    return header;
  }

  // iframe/js/header-silk/workflow.js
  function getRowOffsetSign(row, rows) {
    if (rows.length === 1) {
      return 1;
    }
    const overallMean = rows.reduce((sum, currentRow) => sum + currentRow.meanMinor, 0) / rows.length;
    const offset = row.meanMinor - overallMean;
    if (Math.abs(offset) > 1e-6) {
      return Math.sign(offset);
    }
    return row.index < rows.length / 2 ? -1 : 1;
  }
  function getPlacementDirection(header, row, settings) {
    switch (settings.positionMode) {
      case "top":
        return header.axis.minor;
      case "bottom":
        return { x: -header.axis.minor.x, y: -header.axis.minor.y };
      case "left":
        return { x: -header.axis.major.x, y: -header.axis.major.y };
      case "right":
        return header.axis.major;
      default: {
        const sign = getRowOffsetSign(row, header.rows);
        return {
          x: header.axis.minor.x * sign,
          y: header.axis.minor.y * sign
        };
      }
    }
  }
  function getPlacementRotation(header, settings) {
    if (settings.rotationMode === "auto") {
      return header.textRotation;
    }
    return Number(settings.rotationMode) || 0;
  }
  function getTargetSilkLayer(header, settings) {
    if (settings.layerMode === "top") {
      return PCB_LAYER_TOP_SILK;
    }
    if (settings.layerMode === "bottom") {
      return PCB_LAYER_BOTTOM_SILK;
    }
    return header.defaultTextLayer;
  }
  function getRangeDistributedCenters(startPoint, endPoint, length) {
    const centers = [];
    if (length <= 0) {
      return centers;
    }
    const xDifferenceAbsolute = Math.abs(startPoint.x - endPoint.x);
    const yDifferenceAbsolute = Math.abs(startPoint.y - endPoint.y);
    if (xDifferenceAbsolute >= yDifferenceAbsolute) {
      const difference2 = (startPoint.x - endPoint.x) / length;
      for (let index = 0; index < length; index += 1) {
        centers[index] = {
          x: (startPoint.x - difference2 * index + (startPoint.x - difference2 * (index + 1))) / 2,
          y: (startPoint.y + endPoint.y) / 2
        };
      }
      return centers;
    }
    const difference = (startPoint.y - endPoint.y) / length;
    for (let index = 0; index < length; index += 1) {
      centers[index] = {
        x: (startPoint.x + endPoint.x) / 2,
        y: (startPoint.y - difference * index + (startPoint.y - difference * (index + 1))) / 2
      };
    }
    return centers;
  }
  function getRangeOrientation(startPoint, endPoint) {
    return Math.abs(startPoint.x - endPoint.x) >= Math.abs(startPoint.y - endPoint.y) ? "horizontal" : "vertical";
  }
  function comparePadNumbers2(leftPadNumber, rightPadNumber) {
    return String(leftPadNumber || "").localeCompare(String(rightPadNumber || ""), "en", {
      numeric: true,
      sensitivity: "base"
    });
  }
  function getRangeBounds(startPoint, endPoint) {
    return {
      minX: Math.min(startPoint.x, endPoint.x),
      maxX: Math.max(startPoint.x, endPoint.x),
      minY: Math.min(startPoint.y, endPoint.y),
      maxY: Math.max(startPoint.y, endPoint.y)
    };
  }
  function getRangeMinorCenters(startPoint, endPoint, count) {
    if (count <= 0) {
      return [];
    }
    const orientation = getRangeOrientation(startPoint, endPoint);
    if (count === 1) {
      return [orientation === "horizontal" ? (startPoint.y + endPoint.y) / 2 : (startPoint.x + endPoint.x) / 2];
    }
    const bounds = getRangeBounds(startPoint, endPoint);
    const centers = [];
    if (orientation === "horizontal") {
      const step2 = (bounds.maxY - bounds.minY) / count;
      for (let index = 0; index < count; index += 1) {
        centers.push(bounds.maxY - step2 * (index + 0.5));
      }
      return centers;
    }
    const step = (bounds.maxX - bounds.minX) / count;
    for (let index = 0; index < count; index += 1) {
      centers.push(bounds.minX + step * (index + 0.5));
    }
    return centers;
  }
  function getRangePlacementRotation(startPoint, endPoint, settings) {
    if (settings.rotationMode !== "auto") {
      return Number(settings.rotationMode) || 0;
    }
    return getRangeOrientation(startPoint, endPoint) === "horizontal" ? 0 : 90;
  }
  function getShellItemsFromRange(startPoint, endPoint, settings) {
    if (!settings.includeShell) {
      return [];
    }
    const bounds = getRangeBounds(startPoint, endPoint);
    const lineWidth = clamp(Math.max(Number(settings.strokeWidthMil) || 0, 1), 1, 40);
    const corners = [
      { x: bounds.minX, y: bounds.minY },
      { x: bounds.maxX, y: bounds.minY },
      { x: bounds.maxX, y: bounds.maxY },
      { x: bounds.minX, y: bounds.maxY }
    ];
    return corners.map((startCorner, index) => {
      const endCorner = corners[(index + 1) % corners.length];
      return {
        type: "line",
        startX: startCorner.x,
        startY: startCorner.y,
        endX: endCorner.x,
        endY: endCorner.y,
        points: [startCorner, endCorner],
        lineWidth
      };
    });
  }
  function getOrientedImageSize(imageWidth, imageHeight, rotation) {
    const normalizedRotation = (Math.round(rotation) % 360 + 360) % 360;
    if (normalizedRotation === 90 || normalizedRotation === 270) {
      return {
        width: imageHeight,
        height: imageWidth
      };
    }
    return {
      width: imageWidth,
      height: imageHeight
    };
  }
  function getItemAxisFootprint(item, orientation, rotation) {
    const orientedSize = getOrientedImageSize(item.imageWidth, item.imageHeight, rotation);
    return orientation === "horizontal" ? {
      major: orientedSize.width,
      minor: orientedSize.height
    } : {
      major: orientedSize.height,
      minor: orientedSize.width
    };
  }
  function getAutoShrinkScaleForItem(item, placementContext) {
    if (!placementContext || !Number.isFinite(placementContext.majorStep) || placementContext.majorStep <= 0) {
      return 1;
    }
    const footprint = getItemAxisFootprint(item, placementContext.orientation, placementContext.rotation);
    const availableMajor = placementContext.majorStep * AUTO_SHRINK_GAP_RATIO_MAJOR;
    const availableMinor = placementContext.rowCount > 1 ? placementContext.minorStep * AUTO_SHRINK_GAP_RATIO_MINOR : Number.POSITIVE_INFINITY;
    const scaleByMajor = footprint.major > 0 ? availableMajor / footprint.major : 1;
    const scaleByMinor = footprint.minor > 0 ? availableMinor / footprint.minor : 1;
    const fitScale = Math.min(scaleByMajor, scaleByMinor, 1);
    if (fitScale >= 1) {
      return 1;
    }
    const easedScale = 1 - (1 - fitScale) * AUTO_SHRINK_EASE;
    return clamp(easedScale, MIN_AUTO_SHRINK_SCALE, 1);
  }
  function layoutArtifactsFromRange(artifacts, startPoint, endPoint, settings) {
    const orientation = getRangeOrientation(startPoint, endPoint);
    const rotation = getRangePlacementRotation(startPoint, endPoint, settings);
    const imageItems = artifacts.items.filter((item) => item.type === "image");
    const rowIndexes = [...new Set(imageItems.map((item) => item.rowIndex))].sort((a, b) => a - b);
    const minorCenters = getRangeMinorCenters(startPoint, endPoint, rowIndexes.length);
    const rowCenterByIndex = new Map(rowIndexes.map((rowIndex, index) => [rowIndex, minorCenters[index]]));
    const bounds = getRangeBounds(startPoint, endPoint);
    const majorSpan = orientation === "horizontal" ? Math.max(bounds.maxX - bounds.minX, 0) : Math.max(bounds.maxY - bounds.minY, 0);
    const minorSpan = orientation === "horizontal" ? Math.max(bounds.maxY - bounds.minY, 0) : Math.max(bounds.maxX - bounds.minX, 0);
    const maxItemsPerRow = Math.max(...rowIndexes.map((rowIndex) => imageItems.filter((item) => item.rowIndex === rowIndex).length), 1);
    const placementContext = {
      orientation,
      rotation,
      rowCount: Math.max(rowIndexes.length, 1),
      majorStep: maxItemsPerRow > 0 ? majorSpan / maxItemsPerRow : majorSpan,
      minorStep: rowIndexes.length > 0 ? minorSpan / rowIndexes.length : minorSpan
    };
    const placedItems = [];
    for (const rowIndex of rowIndexes) {
      const rowItems = imageItems.filter((item) => item.rowIndex === rowIndex).sort((a, b) => {
        const padNumberOrder = comparePadNumbers2(a.padNumber, b.padNumber);
        if (padNumberOrder !== 0) {
          return padNumberOrder;
        }
        return a.padIndex - b.padIndex;
      });
      const distributedCenters = getRangeDistributedCenters(startPoint, endPoint, rowItems.length);
      const rowMinorCenter = rowCenterByIndex.get(rowIndex);
      for (let index = 0; index < rowItems.length; index += 1) {
        const item = rowItems[index];
        const distributedCenter = distributedCenters[index];
        const centerX = orientation === "horizontal" ? distributedCenter.x : rowMinorCenter;
        const centerY = orientation === "horizontal" ? rowMinorCenter : distributedCenter.y;
        const autoScale = getAutoShrinkScaleForItem(item, placementContext);
        const scaledImageWidth = item.imageWidth * autoScale;
        const scaledImageHeight = item.imageHeight * autoScale;
        const topLeft = getImageTopLeftFromCenter(
          centerX,
          centerY,
          scaledImageWidth,
          scaledImageHeight,
          rotation
        );
        placedItems.push({
          ...item,
          centerX,
          centerY,
          topLeftX: topLeft.x,
          topLeftY: topLeft.y,
          imageWidth: scaledImageWidth,
          imageHeight: scaledImageHeight,
          rotation,
          autoScale
        });
      }
    }
    return placedItems.concat(getShellItemsFromRange(startPoint, endPoint, settings));
  }
  function getDeleteTargetLayers(header, settings) {
    const layers = /* @__PURE__ */ new Set([header.defaultTextLayer]);
    if (settings.layerMode === "top") {
      layers.add(PCB_LAYER_TOP_SILK);
    } else if (settings.layerMode === "bottom") {
      layers.add(PCB_LAYER_BOTTOM_SILK);
    }
    return Array.from(layers);
  }
  function getDeletionTargetsFromRange(header, startPoint, endPoint) {
    const orientation = getRangeOrientation(startPoint, endPoint);
    const rowIndexes = [...new Set(header.rows.map((row) => row.index))].sort((a, b) => a - b);
    const rowPads = rowIndexes.map((rowIndex) => {
      const row = header.rows.find((item) => item.index === rowIndex);
      const pads = row ? [...row.pads].sort((leftPad, rightPad) => {
        const padNumberOrder = comparePadNumbers2(leftPad.padNumber, rightPad.padNumber);
        if (padNumberOrder !== 0) {
          return padNumberOrder;
        }
        return leftPad.majorProjection - rightPad.majorProjection;
      }) : [];
      return {
        rowIndex,
        pads
      };
    });
    const rowCount = rowPads.length || 1;
    const minorCenters = getRangeMinorCenters(startPoint, endPoint, rowCount);
    const rowCenterByIndex = new Map(rowIndexes.map((rowIndex, index) => [rowIndex, minorCenters[index]]));
    const targets = [];
    for (const rowItem of rowPads) {
      const distributedCenters = getRangeDistributedCenters(startPoint, endPoint, rowItem.pads.length);
      const rowMinorCenter = rowCenterByIndex.get(rowItem.rowIndex);
      for (let index = 0; index < rowItem.pads.length; index += 1) {
        const distributedCenter = distributedCenters[index];
        targets.push({
          rowIndex: rowItem.rowIndex,
          padNumber: rowItem.pads[index].padNumber,
          centerX: orientation === "horizontal" ? distributedCenter.x : rowMinorCenter,
          centerY: orientation === "horizontal" ? rowMinorCenter : distributedCenter.y
        });
      }
    }
    const bounds = getRangeBounds(startPoint, endPoint);
    const majorSpan = orientation === "horizontal" ? bounds.maxX - bounds.minX : bounds.maxY - bounds.minY;
    const minorSpan = orientation === "horizontal" ? bounds.maxY - bounds.minY : bounds.maxX - bounds.minX;
    const maxItemsInRow = Math.max(...rowPads.map((rowItem) => rowItem.pads.length), 1);
    const longitudinalTolerance = clamp(majorSpan / Math.max(maxItemsInRow, 1) * 0.38, 8, 28);
    const crossTolerance = rowCount > 1 ? clamp(minorSpan / rowCount * 0.42, 8, 28) : clamp(header.nominalPitch * 0.45, 8, 24);
    const tolerance = Math.max(longitudinalTolerance, crossTolerance);
    return {
      targets,
      shellItems: getShellItemsFromRange(startPoint, endPoint, {
        includeShell: true,
        strokeWidthMil: Math.max(header.nominalPitch * 0.18, 4)
      }),
      tolerance
    };
  }
  function getHeaderShellItems(header, settings) {
    if (!settings.includeShell) {
      return [];
    }
    const lineWidth = clamp(Math.max(Number(settings.strokeWidthMil) || 0, 1), 1, 40);
    const bounds = header.shellBounds;
    const corners = [
      projectPoint(header.axis, bounds.majorMin, bounds.minorMin),
      projectPoint(header.axis, bounds.majorMax, bounds.minorMin),
      projectPoint(header.axis, bounds.majorMax, bounds.minorMax),
      projectPoint(header.axis, bounds.majorMin, bounds.minorMax)
    ];
    return corners.map((startPoint, index) => {
      const endPoint = corners[(index + 1) % corners.length];
      return {
        type: "line",
        startX: startPoint.x,
        startY: startPoint.y,
        endX: endPoint.x,
        endY: endPoint.y,
        points: [startPoint, endPoint],
        lineWidth
      };
    });
  }
  async function buildHeaderArtifacts(header, settings) {
    const targetLayer = getTargetSilkLayer(header, settings);
    const artifacts = [];
    const textPlanCache = /* @__PURE__ */ new Map();
    const labelMappings = parseLabelMappings(settings && settings.labelMapText);
    const offsetMil = clamp(Number(settings.offsetMil) || 18, 1, 300);
    const rotation = getPlacementRotation(header, settings);
    for (const row of header.rows) {
      const direction = getPlacementDirection(header, row, settings);
      for (let padIndex = 0; padIndex < row.pads.length; padIndex += 1) {
        const pad = row.pads[padIndex];
        const displayLabel = resolveHeaderPadDisplayLabel(header, pad, labelMappings).displayLabel;
        if (!displayLabel) {
          continue;
        }
        const placement = {
          text: displayLabel,
          x: pad.x + direction.x * offsetMil,
          y: pad.y + direction.y * offsetMil,
          rotation
        };
        if (!textPlanCache.has(placement.text)) {
          textPlanCache.set(placement.text, getTextRenderPlan(placement.text, settings));
        }
        const textPlan = textPlanCache.get(placement.text);
        const topLeft = getImageTopLeftFromCenter(
          placement.x,
          placement.y,
          textPlan.imageWidthMil,
          textPlan.imageHeightMil,
          placement.rotation
        );
        artifacts.push({
          type: "image",
          text: placement.text,
          padNumber: pad.padNumber,
          rowIndex: row.index,
          padIndex,
          centerX: placement.x,
          centerY: placement.y,
          topLeftX: topLeft.x,
          topLeftY: topLeft.y,
          imageWidth: textPlan.imageWidthMil,
          imageHeight: textPlan.imageHeightMil,
          horizonMirror: false,
          rotation: placement.rotation
        });
      }
    }
    return {
      layer: targetLayer,
      header,
      items: artifacts.concat(getHeaderShellItems(header, settings))
    };
  }
  async function prepareTextComplexPolygon(text, settings) {
    const plan = getTextRenderPlan(text, settings);
    const rendered = await renderTextToBlob(text, settings, plan);
    const complexPolygon = await eda.pcb_MathPolygon.convertImageToComplexPolygon(
      rendered.blob,
      rendered.widthPx,
      rendered.heightPx,
      0.3,
      0.9,
      1,
      2,
      false,
      false
    );
    if (!complexPolygon) {
      throw new Error("生成失败，请调整参数后重试。");
    }
    return complexPolygon;
  }
  function createTextAssetCacheKey(text, settings) {
    const textSettings = getTextRenderSettings(settings);
    const fontFamily = normalizeText(settings && settings.fontFamily) || DEFAULT_SETTINGS.fontFamily;
    return [
      text,
      fontFamily,
      textSettings.fontSizeMil.toFixed(3),
      textSettings.strokeWidthMil.toFixed(3),
      settings && settings.invert ? "invert" : "normal"
    ].join("|");
  }
  async function getOrPrepareTextComplexPolygon(text, settings) {
    const cacheKey = createTextAssetCacheKey(text, settings);
    const cachedEntry = textAssetCache.get(cacheKey);
    if (cachedEntry) {
      touchMapEntry(textAssetCache, cacheKey, cachedEntry);
      return cachedEntry;
    }
    const pendingEntry = prepareTextComplexPolygon(text, settings).then((complexPolygon) => {
      touchMapEntry(textAssetCache, cacheKey, Promise.resolve(complexPolygon));
      trimMapSize(textAssetCache, MAX_TEXT_ASSET_CACHE, cacheKey);
      return complexPolygon;
    }).catch((error) => {
      textAssetCache.delete(cacheKey);
      throw error;
    });
    touchMapEntry(textAssetCache, cacheKey, pendingEntry);
    trimMapSize(textAssetCache, MAX_TEXT_ASSET_CACHE, cacheKey);
    return pendingEntry;
  }
  async function prepareTextAssetMap(items, settings) {
    const imageItems = items.filter((item) => item.type === "image");
    if (!imageItems.length) {
      return /* @__PURE__ */ new Map();
    }
    const uniqueTexts = [...new Set(imageItems.map((item) => item.text).filter(Boolean))];
    const assetEntries = await Promise.all(uniqueTexts.map(async (text) => {
      const complexPolygon = await getOrPrepareTextComplexPolygon(text, settings);
      return [text, complexPolygon];
    }));
    return new Map(assetEntries);
  }
  function hydratePlacedItemsWithAssetMap(items, assetMap) {
    return items.map((item) => {
      if (item.type !== "image") {
        return item;
      }
      return {
        ...item,
        complexPolygon: assetMap.get(item.text)
      };
    });
  }
  function waitForRangeSelection(options) {
    return new Promise((resolve, reject) => {
      const selectionOptions = options || {};
      const followMouseTip = selectionOptions.followMouseTip || "请在 PCB 画布上框选生成范围。";
      const toastMessage = selectionOptions.toastMessage || "请在 PCB 中框选生成范围。";
      const tooSmallMessage = selectionOptions.tooSmallMessage || "请拖动框选一个范围，不要单击。";
      const rangePoints = [];
      let finished = false;
      function cleanup() {
        try {
          eda.pcb_Event.removeEventListener(RANGE_SELECTION_EVENT_ID);
        } catch (e) {
        }
        void eda.sys_Message.removeFollowMouseTip(followMouseTip).catch(() => {
        });
      }
      if (eda.pcb_Event.isEventListenerAlreadyExist(RANGE_SELECTION_EVENT_ID)) {
        eda.pcb_Event.removeEventListener(RANGE_SELECTION_EVENT_ID);
      }
      void eda.sys_Message.showFollowMouseTip(followMouseTip).catch(() => {
      });
      eda.sys_Message.showToastMessage(toastMessage, ESYS_ToastMessageType.INFO, 3);
      eda.pcb_Event.addMouseEventListener(RANGE_SELECTION_EVENT_ID, "selected", async () => {
        if (finished) {
          return;
        }
        try {
          const currentMousePosition = await eda.pcb_SelectControl.getCurrentMousePosition();
          if (!currentMousePosition) {
            return;
          }
          rangePoints.push({
            x: Number(currentMousePosition.x) || 0,
            y: Number(currentMousePosition.y) || 0
          });
          if (rangePoints.length < 2) {
            return;
          }
          const startPoint = rangePoints[0];
          const endPoint = rangePoints[1];
          if (Math.hypot(startPoint.x - endPoint.x, startPoint.y - endPoint.y) < MIN_RANGE_SELECTION_DISTANCE) {
            rangePoints.length = 0;
            eda.sys_Message.showToastMessage(tooSmallMessage, ESYS_ToastMessageType.WARNING, 3);
            return;
          }
          finished = true;
          cleanup();
          resolve({
            startPoint,
            endPoint
          });
        } catch (error) {
          finished = true;
          cleanup();
          reject(error);
        }
      }, false);
    });
  }
  async function createCombinedSilkAtMouse(artifacts, settings) {
    const assetMapPromise = prepareTextAssetMap(artifacts.items, settings);
    const selection = await waitForRangeSelection({
      followMouseTip: "请在 PCB 画布上框选生成范围。",
      toastMessage: "请在 PCB 中框选生成范围。",
      tooSmallMessage: "请拖动框选一个范围，不要单击。"
    });
    const translatedItems = layoutArtifactsFromRange(artifacts, selection.startPoint, selection.endPoint, settings);
    const assetMap = await assetMapPromise;
    const finalItems = hydratePlacedItemsWithAssetMap(translatedItems, assetMap);
    const autoAdjustedCount = finalItems.filter((item) => item.type === "image" && Number(item.autoScale) < 0.999).length;
    const finalHandles = await createFinalGroup(artifacts.layer, finalItems, false);
    if (!finalHandles.length) {
      throw new Error("生成失败，请重试。");
    }
    try {
      const totalCreated = await finalizePlacedGroup(artifacts.layer, finalItems, finalHandles);
      return {
        totalCreated,
        handles: finalHandles,
        autoAdjustedCount
      };
    } catch (error) {
      await deleteFinalGroup(finalHandles).catch(() => {
      });
      throw error;
    }
  }
  function findNearestImageForTarget(existingImages, usedPrimitiveIds, target, tolerance) {
    let bestMatch;
    for (const existingImage of existingImages) {
      const primitiveId = normalizeText(existingImage.getState_PrimitiveId && existingImage.getState_PrimitiveId());
      if (!primitiveId || usedPrimitiveIds.has(primitiveId)) {
        continue;
      }
      const center = getImageCenterFromTopLeft(
        Number(existingImage.getState_X && existingImage.getState_X()) || 0,
        Number(existingImage.getState_Y && existingImage.getState_Y()) || 0,
        Number(existingImage.getState_Width && existingImage.getState_Width()) || 0,
        Number(existingImage.getState_Height && existingImage.getState_Height()) || 0,
        Number(existingImage.getState_Rotation && existingImage.getState_Rotation()) || 0
      );
      const distance = Math.hypot(center.x - target.centerX, center.y - target.centerY);
      if (distance > tolerance) {
        continue;
      }
      if (!bestMatch || distance < bestMatch.distance) {
        bestMatch = {
          distance,
          primitiveId,
          primitive: existingImage
        };
      }
    }
    return bestMatch;
  }
  function findNearestLineForTarget(existingLines, usedPrimitiveIds, target, tolerance) {
    let bestMatch;
    for (const existingLine of existingLines) {
      const primitiveId = normalizeText(existingLine.getState_PrimitiveId && existingLine.getState_PrimitiveId());
      if (!primitiveId || usedPrimitiveIds.has(primitiveId)) {
        continue;
      }
      const sameDirection = Math.hypot(
        (Number(existingLine.getState_StartX && existingLine.getState_StartX()) || 0) - target.startX,
        (Number(existingLine.getState_StartY && existingLine.getState_StartY()) || 0) - target.startY
      ) + Math.hypot(
        (Number(existingLine.getState_EndX && existingLine.getState_EndX()) || 0) - target.endX,
        (Number(existingLine.getState_EndY && existingLine.getState_EndY()) || 0) - target.endY
      );
      const reverseDirection = Math.hypot(
        (Number(existingLine.getState_StartX && existingLine.getState_StartX()) || 0) - target.endX,
        (Number(existingLine.getState_StartY && existingLine.getState_StartY()) || 0) - target.endY
      ) + Math.hypot(
        (Number(existingLine.getState_EndX && existingLine.getState_EndX()) || 0) - target.startX,
        (Number(existingLine.getState_EndY && existingLine.getState_EndY()) || 0) - target.startY
      );
      const distance = Math.min(sameDirection, reverseDirection);
      if (distance > tolerance * 2) {
        continue;
      }
      if (!bestMatch || distance < bestMatch.distance) {
        bestMatch = {
          distance,
          primitiveId,
          primitive: existingLine
        };
      }
    }
    return bestMatch;
  }
  async function deleteArtifactsByRangeSelection(header, settings) {
    const selection = await waitForRangeSelection({
      followMouseTip: "请在 PCB 画布上框选要删除的丝印范围。",
      toastMessage: "请在 PCB 中框选要删除的丝印范围。",
      tooSmallMessage: "请拖动框选要删除的范围，不要单击。"
    });
    const deletionTargets = getDeletionTargetsFromRange(header, selection.startPoint, selection.endPoint);
    const layers = getDeleteTargetLayers(header, settings);
    let deletedCount = 0;
    for (const layer of layers) {
      const existingImages = asArray(await eda.pcb_PrimitiveImage.getAll(layer));
      const existingLines = asArray(await eda.pcb_PrimitiveLine.getAll(void 0, layer));
      const usedImageIds = /* @__PURE__ */ new Set();
      const usedLineIds = /* @__PURE__ */ new Set();
      const imagesToDelete = [];
      const linesToDelete = [];
      for (const target of deletionTargets.targets) {
        const match = findNearestImageForTarget(existingImages, usedImageIds, target, deletionTargets.tolerance);
        if (!match) {
          continue;
        }
        usedImageIds.add(match.primitiveId);
        imagesToDelete.push(match.primitive);
      }
      for (const shellItem of deletionTargets.shellItems) {
        const match = findNearestLineForTarget(existingLines, usedLineIds, shellItem, deletionTargets.tolerance);
        if (!match) {
          continue;
        }
        usedLineIds.add(match.primitiveId);
        linesToDelete.push(match.primitive);
      }
      if (imagesToDelete.length) {
        await eda.pcb_PrimitiveImage.delete(imagesToDelete);
        deletedCount += imagesToDelete.length;
      }
      if (linesToDelete.length) {
        await eda.pcb_PrimitiveLine.delete(linesToDelete);
        deletedCount += linesToDelete.length;
      }
    }
    return deletedCount;
  }
  async function createFinalHandle(layer, item, primitiveLock) {
    if (item.type === "image") {
      const createdImage = await eda.pcb_PrimitiveImage.create(
        item.topLeftX,
        item.topLeftY,
        item.complexPolygon,
        layer,
        item.imageWidth,
        item.imageHeight,
        item.rotation,
        item.horizonMirror,
        Boolean(primitiveLock)
      );
      return createdImage ? { type: "image", primitive: createdImage } : void 0;
    }
    const createdLine = await eda.pcb_PrimitiveLine.create(
      "",
      layer,
      item.startX,
      item.startY,
      item.endX,
      item.endY,
      item.lineWidth,
      Boolean(primitiveLock)
    );
    return createdLine ? { type: "line", primitive: createdLine } : void 0;
  }
  async function createFinalGroup(layer, items, primitiveLock) {
    const handles = [];
    for (let index = 0; index < items.length; index += CREATE_BATCH_SIZE) {
      const batchItems = items.slice(index, index + CREATE_BATCH_SIZE);
      const batchHandles = await Promise.all(
        batchItems.map((item) => createFinalHandle(layer, item, primitiveLock))
      );
      for (const handle of batchHandles) {
        if (handle) {
          handles.push(handle);
        }
      }
    }
    return handles;
  }
  async function deleteFinalGroup(handles) {
    if (!handles.length) {
      return;
    }
    const images = handles.filter((handle) => handle.type === "image").map((handle) => handle.primitive);
    const lines = handles.filter((handle) => handle.type === "line").map((handle) => handle.primitive);
    if (images.length) {
      await eda.pcb_PrimitiveImage.delete(images);
    }
    if (lines.length) {
      await eda.pcb_PrimitiveLine.delete(lines);
    }
  }
  function getExcludedPrimitiveIds(handles) {
    return new Set(
      handles.map((handle) => handle && handle.primitive && handle.primitive.getState_PrimitiveId && handle.primitive.getState_PrimitiveId()).filter(Boolean)
    );
  }
  async function deleteExistingArtifacts(layer, items, excludedPrimitiveIds) {
    const imageItems = items.filter((item) => item.type === "image");
    const lineItems = items.filter((item) => item.type === "line");
    let deletedCount = 0;
    if (imageItems.length) {
      const existingImages = asArray(await eda.pcb_PrimitiveImage.getAll(layer));
      const imagesToDelete = [];
      for (const existingImage of existingImages) {
        const primitiveId = existingImage.getState_PrimitiveId && existingImage.getState_PrimitiveId();
        if (excludedPrimitiveIds && primitiveId && excludedPrimitiveIds.has(primitiveId)) {
          continue;
        }
        const existingX = Number(existingImage.getState_X && existingImage.getState_X());
        const existingY = Number(existingImage.getState_Y && existingImage.getState_Y());
        const existingWidth = Number(existingImage.getState_Width && existingImage.getState_Width());
        const existingHeight = Number(existingImage.getState_Height && existingImage.getState_Height());
        const existingRotation = Number(existingImage.getState_Rotation && existingImage.getState_Rotation());
        const matchedItem = imageItems.find((item) => {
          if (Math.abs(existingRotation - item.rotation) > 1) {
            return false;
          }
          const tolerance = Math.max(Math.min(item.imageWidth, item.imageHeight) * 0.2, 1);
          if (Math.abs(existingWidth - item.imageWidth) > tolerance) {
            return false;
          }
          if (Math.abs(existingHeight - item.imageHeight) > tolerance) {
            return false;
          }
          return Math.hypot(existingX - item.topLeftX, existingY - item.topLeftY) <= tolerance;
        });
        if (matchedItem) {
          imagesToDelete.push(existingImage);
        }
      }
      if (imagesToDelete.length) {
        await eda.pcb_PrimitiveImage.delete(imagesToDelete);
        deletedCount += imagesToDelete.length;
      }
    }
    if (lineItems.length) {
      const existingLines = asArray(await eda.pcb_PrimitiveLine.getAll(void 0, layer));
      const linesToDelete = [];
      for (const existingLine of existingLines) {
        const primitiveId = existingLine.getState_PrimitiveId && existingLine.getState_PrimitiveId();
        if (excludedPrimitiveIds && primitiveId && excludedPrimitiveIds.has(primitiveId)) {
          continue;
        }
        const matchedItem = lineItems.find((item) => {
          const tolerance = Math.max(item.lineWidth * 0.5, 1);
          const sameDirection = Math.hypot(existingLine.getState_StartX() - item.startX, existingLine.getState_StartY() - item.startY) <= tolerance && Math.hypot(existingLine.getState_EndX() - item.endX, existingLine.getState_EndY() - item.endY) <= tolerance;
          const reverseDirection = Math.hypot(existingLine.getState_StartX() - item.endX, existingLine.getState_StartY() - item.endY) <= tolerance && Math.hypot(existingLine.getState_EndX() - item.startX, existingLine.getState_EndY() - item.startY) <= tolerance;
          return (sameDirection || reverseDirection) && Math.abs(existingLine.getState_LineWidth() - item.lineWidth) <= tolerance;
        });
        if (matchedItem) {
          linesToDelete.push(existingLine);
        }
      }
      if (linesToDelete.length) {
        await eda.pcb_PrimitiveLine.delete(linesToDelete);
        deletedCount += linesToDelete.length;
      }
    }
    return deletedCount;
  }
  async function finalizePlacedGroup(layer, items, handles) {
    await deleteExistingArtifacts(layer, items, getExcludedPrimitiveIds(handles));
    return handles.length;
  }
  async function resolveSelectedHeader() {
    const components = await getSelectedComponents();
    const headers = (await Promise.all(components.map(buildHeaderComponent))).filter(Boolean);
    if (!components.length) {
      throw new Error("请先选中一个排针。");
    }
    if (!headers.length) {
      throw new Error("未找到可用的焊盘。");
    }
    if (headers.length > 1) {
      throw new Error("请只选择一个排针。");
    }
    return headers[0];
  }
  async function startPlacement() {
    syncSettingsFromForm();
    try {
      const placementSettings = { ...appState.currentSettings };
      const header = await resolveSelectedHeader();
      const artifacts = await buildHeaderArtifacts(header, placementSettings);
      if (!artifacts.items.length) {
        eda.sys_Message.showToastMessage("没有可生成的丝印。", ESYS_ToastMessageType.WARNING, 3);
        return;
      }
      const result = await createCombinedSilkAtMouse(artifacts, placementSettings);
      await rememberGeneratedArtifacts(header, artifacts.layer, result.handles).catch(() => {
      });
      const autoShrinkText = result.autoAdjustedCount > 0 ? `，其中 ${result.autoAdjustedCount} 个丝印按密度自动缩小` : "";
      eda.sys_Message.showToastMessage(`已按框选范围生成，共 ${result.totalCreated} 个图元${autoShrinkText}。`, ESYS_ToastMessageType.SUCCESS, 2);
      void eda.pcb_SelectControl.clearSelected().catch(() => {
      });
    } catch (error) {
      const message = error instanceof Error ? error.message : String(error);
      eda.sys_Message.showToastMessage(message, ESYS_ToastMessageType.ERROR, 4);
    }
  }
  async function startDeleteGenerated() {
    syncSettingsFromForm();
    try {
      const deleteSettings = { ...appState.currentSettings };
      const header = await resolveSelectedHeader();
      const scope = await getCurrentDocumentScope();
      const storedGroups = loadArtifactGroups();
      const matchedGroups = storedGroups.filter((group) => doesArtifactGroupMatchHeader(group, scope, header));
      let deletedCount = 0;
      if (matchedGroups.length) {
        for (const group of matchedGroups) {
          deletedCount += await deleteArtifactGroupByRecord(group);
        }
        saveArtifactGroups(storedGroups.filter((group) => !doesArtifactGroupMatchHeader(group, scope, header)));
      } else {
        deletedCount = await deleteArtifactsByRangeSelection(header, deleteSettings);
      }
      if (!deletedCount) {
        eda.sys_Message.showToastMessage("没有找到可删除的丝印。", ESYS_ToastMessageType.WARNING, 3);
        return;
      }
      eda.sys_Message.showToastMessage(`已删除 ${deletedCount} 个图元。`, ESYS_ToastMessageType.SUCCESS, 2);
      void eda.pcb_SelectControl.clearSelected().catch(() => {
      });
    } catch (error) {
      const message = error instanceof Error ? error.message : String(error);
      eda.sys_Message.showToastMessage(message, ESYS_ToastMessageType.ERROR, 4);
    }
  }

  // iframe/js/header-silk/panel.js
  function activateTab(tabName) {
    elements2.tabs.forEach((tabButton) => {
      const isActive = tabButton.dataset.tabTarget === tabName;
      tabButton.classList.toggle("is-active", isActive);
      tabButton.setAttribute("aria-selected", isActive ? "true" : "false");
    });
    elements2.tabPanels.forEach((tabPanel) => {
      const isActive = tabPanel.dataset.tabPanel === tabName;
      tabPanel.classList.toggle("is-active", isActive);
      tabPanel.hidden = !isActive;
    });
  }
  function refreshUnitBadges(unitMode) {
    elements2.fontSizeUnit.textContent = unitMode;
    elements2.strokeWidthUnit.textContent = unitMode;
    elements2.offsetUnit.textContent = unitMode;
  }
  function refreshNumericConstraints(unitMode) {
    if (unitMode === "mm") {
      elements2.fontSize.min = "0.254";
      elements2.fontSize.step = "0.01";
      elements2.strokeWidth.min = "0";
      elements2.strokeWidth.step = "0.01";
      elements2.offset.min = "0.0254";
      elements2.offset.step = "0.01";
      return;
    }
    elements2.fontSize.min = "10";
    elements2.fontSize.step = "1";
    elements2.strokeWidth.min = "0";
    elements2.strokeWidth.step = "0.1";
    elements2.offset.min = "1";
    elements2.offset.step = "1";
  }
  function applySettings(settings) {
    elements2.fontFamily.value = settings.fontFamily;
    elements2.unitMode.value = settings.unitMode;
    elements2.layerMode.value = settings.layerMode;
    refreshNumericConstraints(settings.unitMode);
    elements2.fontSize.value = formatNumeric(toDisplayValue(settings.fontSizeMil, settings.unitMode));
    elements2.strokeWidth.value = formatNumeric(toDisplayValue(settings.strokeWidthMil, settings.unitMode));
    elements2.positionMode.value = settings.positionMode;
    elements2.rotationMode.value = settings.rotationMode;
    elements2.offset.value = formatNumeric(toDisplayValue(settings.offsetMil, settings.unitMode));
    elements2.labelMap.value = settings.labelMapText || "";
    elements2.includeShell.checked = Boolean(settings.includeShell);
    elements2.invert.checked = Boolean(settings.invert);
    refreshUnitBadges(settings.unitMode);
    renderPreview();
  }
  function syncSettingsFromForm2() {
    appState.currentSettings.fontFamily = elements2.fontFamily.value || DEFAULT_SETTINGS2.fontFamily;
    appState.currentSettings.layerMode = elements2.layerMode.value || DEFAULT_SETTINGS2.layerMode;
    appState.currentSettings.fontSizeMil = clamp(fromDisplayValue(Number(elements2.fontSize.value) || 0, appState.currentSettings.unitMode), 10, 240);
    appState.currentSettings.strokeWidthMil = clamp(fromDisplayValue(Number(elements2.strokeWidth.value) || 0, appState.currentSettings.unitMode), 0, 80);
    appState.currentSettings.positionMode = elements2.positionMode.value || DEFAULT_SETTINGS2.positionMode;
    appState.currentSettings.rotationMode = elements2.rotationMode.value || DEFAULT_SETTINGS2.rotationMode;
    appState.currentSettings.offsetMil = clamp(fromDisplayValue(Number(elements2.offset.value) || 0, appState.currentSettings.unitMode), 1, 300);
    appState.currentSettings.labelMapText = elements2.labelMap.value || "";
    appState.currentSettings.includeShell = elements2.includeShell.checked;
    appState.currentSettings.invert = elements2.invert.checked;
    saveSettings(appState.currentSettings);
  }
  function renderLabelPreviewState(metaText, chips, options) {
    const meta = elements2.labelPreviewMeta;
    const list = elements2.labelPreviewList;
    if (!meta || !list) {
      return;
    }
    meta.textContent = metaText;
    list.innerHTML = "";
    for (const chip of chips) {
      if (chip && chip.editable) {
        const rowElement = document.createElement("label");
        rowElement.className = chip.hasManualOverride ? "label-edit-row is-manual" : "label-edit-row";
        const metaElement = document.createElement("div");
        metaElement.className = "label-edit-meta";
        const padElement = document.createElement("strong");
        padElement.textContent = chip.padNumber || "?";
        const sourceElement = document.createElement("span");
        sourceElement.textContent = chip.netName || chip.autoLabel || "未命名网络";
        sourceElement.title = chip.netName || chip.autoLabel || "未命名网络";
        metaElement.appendChild(padElement);
        metaElement.appendChild(sourceElement);
        const inputElement = document.createElement("input");
        inputElement.className = chip.hasManualOverride ? "label-edit-input is-manual" : "label-edit-input";
        inputElement.type = "text";
        inputElement.value = chip.displayLabel || "";
        inputElement.placeholder = chip.autoLabel || chip.displayLabel || "";
        inputElement.title = chip.autoLabel ? `默认标签：${chip.autoLabel}` : "默认标签";
        const syncManualState = (restoreAutoWhenEmpty) => {
          const normalizedInput = normalizeText(inputElement.value);
          if (!normalizedInput) {
            setManualLabelOverrideByKey(chip.componentId, chip.padKey, "", chip.autoLabel);
            rowElement.className = "label-edit-row";
            inputElement.className = "label-edit-input";
            if (restoreAutoWhenEmpty) {
              inputElement.value = chip.autoLabel || "";
            }
            return;
          }
          const storedLabel = setManualLabelOverrideByKey(
            chip.componentId,
            chip.padKey,
            normalizedInput,
            chip.autoLabel
          );
          const isManual = Boolean(storedLabel);
          rowElement.className = isManual ? "label-edit-row is-manual" : "label-edit-row";
          inputElement.className = isManual ? "label-edit-input is-manual" : "label-edit-input";
          if (!isManual) {
            inputElement.value = chip.autoLabel || "";
          }
        };
        inputElement.addEventListener("input", () => syncManualState(false));
        inputElement.addEventListener("change", () => syncManualState(true));
        inputElement.addEventListener("blur", () => syncManualState(true));
        rowElement.appendChild(metaElement);
        rowElement.appendChild(inputElement);
        list.appendChild(rowElement);
        continue;
      }
      const chipElement = document.createElement("span");
      chipElement.className = chip.muted ? "label-chip is-muted" : "label-chip";
      if (chip.padNumber) {
        const padElement = document.createElement("strong");
        padElement.textContent = chip.padNumber;
        chipElement.appendChild(padElement);
        chipElement.appendChild(document.createTextNode(`:${chip.text}`));
      } else {
        chipElement.textContent = chip.text;
      }
      list.appendChild(chipElement);
    }
    if (!chips.length && options && options.emptyText) {
      const chipElement = document.createElement("span");
      chipElement.className = "label-chip is-muted";
      chipElement.textContent = options.emptyText;
      list.appendChild(chipElement);
    }
  }
  async function refreshLabelPreview() {
    const requestId = ++appState.labelPreviewRequestId;
    renderLabelPreviewState("正在读取当前排针...", [], { emptyText: "读取中" });
    try {
      const header = await resolveSelectedHeader();
      if (requestId !== appState.labelPreviewRequestId) {
        return;
      }
      const labelItems = getHeaderDisplayLabelItems(header, appState.currentSettings);
      const manualOverrideCount = labelItems.filter((item) => item.hasManualOverride).length;
      const visibleItems = labelItems.map((item) => ({
        editable: true,
        componentId: item.componentId,
        padKey: item.padKey,
        padNumber: item.padNumber,
        netName: item.netName,
        autoLabel: item.autoLabel || item.netName || `P${item.padNumber || "?"}`,
        displayLabel: item.displayLabel || item.netName || `P${item.padNumber || "?"}`,
        hasManualOverride: item.hasManualOverride
      }));
      renderLabelPreviewState(
        `${header.designator} · ${header.padCount} Pin · ${header.rows.length} 行 · 手改 ${manualOverrideCount} 项（自动保存）`,
        visibleItems,
        { emptyText: "没有可显示的标签" }
      );
    } catch (error) {
      if (requestId !== appState.labelPreviewRequestId) {
        return;
      }
      const message = error instanceof Error ? error.message : String(error);
      renderLabelPreviewState(message, [], { emptyText: "等待选择" });
    }
  }
  function scheduleLabelPreviewRefresh(delayMs) {
    if (appState.labelPreviewRefreshTimer) {
      clearTimeout(appState.labelPreviewRefreshTimer);
    }
    appState.labelPreviewRefreshTimer = setTimeout(() => {
      appState.labelPreviewRefreshTimer = void 0;
      void refreshLabelPreview();
    }, delayMs == null ? 180 : delayMs);
  }
  async function getSelectedComponentSignature() {
    if (!eda.pcb_SelectControl) {
      return "";
    }
    const selectedPrimitives = asArray(await eda.pcb_SelectControl.getAllSelectedPrimitives());
    const componentIds = /* @__PURE__ */ new Set();
    for (const primitive of selectedPrimitives) {
      const primitiveType = String(primitive.getState_PrimitiveType && primitive.getState_PrimitiveType());
      if (primitiveType === "Component") {
        componentIds.add(normalizeText(primitive.getState_PrimitiveId && primitive.getState_PrimitiveId()));
        continue;
      }
      if (primitiveType === "ComponentPad") {
        componentIds.add(normalizeText(primitive.getState_ParentComponentPrimitiveId && primitive.getState_ParentComponentPrimitiveId()));
      }
    }
    return [...componentIds].filter(Boolean).sort().join("|");
  }
  function startLabelPreviewSelectionWatcher() {
    if (appState.labelPreviewSelectionWatcher) {
      clearInterval(appState.labelPreviewSelectionWatcher);
    }
    let pollInFlight = false;
    appState.labelPreviewSelectionWatcher = setInterval(async () => {
      if (pollInFlight) {
        return;
      }
      pollInFlight = true;
      try {
        const selectionSignature = await getSelectedComponentSignature();
        if (selectionSignature === appState.lastLabelPreviewSelectionSignature) {
          return;
        }
        appState.lastLabelPreviewSelectionSignature = selectionSignature;
        scheduleLabelPreviewRefresh(0);
      } catch (e) {
      } finally {
        pollInFlight = false;
      }
    }, LABEL_PREVIEW_SELECTION_POLL_MS);
  }
  async function populateFontOptions(settings) {
    const fallbackFonts = ["黑体", "宋体", "微软雅黑", "Arial", "sans-serif"];
    let fonts = fallbackFonts;
    try {
      const remoteFonts = await eda.sys_FontManager.getFontsList();
      if (Array.isArray(remoteFonts) && remoteFonts.length > 0) {
        fonts = remoteFonts;
      }
    } catch (error) {
      console.error(error);
    }
    const seen = /* @__PURE__ */ new Set();
    elements2.fontFamily.innerHTML = "";
    for (const fontName of fonts.concat(fallbackFonts)) {
      const normalized = normalizeText(fontName);
      if (!normalized || seen.has(normalized)) {
        continue;
      }
      seen.add(normalized);
      const option = document.createElement("option");
      option.value = normalized;
      option.textContent = normalized;
      elements2.fontFamily.appendChild(option);
    }
    if (!seen.has(settings.fontFamily)) {
      const option = document.createElement("option");
      option.value = settings.fontFamily;
      option.textContent = settings.fontFamily;
      elements2.fontFamily.appendChild(option);
    }
    elements2.fontFamily.value = settings.fontFamily;
  }
  function bindSettings() {
    elements2.tabs.forEach((tabButton) => {
      tabButton.addEventListener("click", () => {
        const tabName = tabButton.dataset.tabTarget || "compact";
        activateTab(tabName);
        if (tabName === "layout") {
          scheduleLabelPreviewRefresh(0);
        }
      });
    });
    elements2.fontFamily.addEventListener("change", () => {
      appState.currentSettings.fontFamily = elements2.fontFamily.value || DEFAULT_SETTINGS2.fontFamily;
      saveSettings(appState.currentSettings);
      renderPreview();
    });
    elements2.unitMode.addEventListener("change", () => {
      syncSettingsFromForm2();
      appState.currentSettings.unitMode = elements2.unitMode.value === "mm" ? "mm" : "mil";
      saveSettings(appState.currentSettings);
      applySettings(appState.currentSettings);
    });
    elements2.layerMode.addEventListener("change", () => {
      appState.currentSettings.layerMode = elements2.layerMode.value || DEFAULT_SETTINGS2.layerMode;
      saveSettings(appState.currentSettings);
      renderPreview();
    });
    elements2.fontSize.addEventListener("input", () => {
      appState.currentSettings.fontSizeMil = clamp(fromDisplayValue(Number(elements2.fontSize.value) || 0, appState.currentSettings.unitMode), 10, 240);
      saveSettings(appState.currentSettings);
      renderPreview();
    });
    elements2.strokeWidth.addEventListener("input", () => {
      appState.currentSettings.strokeWidthMil = clamp(fromDisplayValue(Number(elements2.strokeWidth.value) || 0, appState.currentSettings.unitMode), 0, 80);
      saveSettings(appState.currentSettings);
      renderPreview();
    });
    elements2.positionMode.addEventListener("change", () => {
      appState.currentSettings.positionMode = elements2.positionMode.value || DEFAULT_SETTINGS2.positionMode;
      saveSettings(appState.currentSettings);
      renderPreview();
    });
    elements2.rotationMode.addEventListener("change", () => {
      appState.currentSettings.rotationMode = elements2.rotationMode.value || DEFAULT_SETTINGS2.rotationMode;
      saveSettings(appState.currentSettings);
      renderPreview();
    });
    elements2.offset.addEventListener("input", () => {
      appState.currentSettings.offsetMil = clamp(fromDisplayValue(Number(elements2.offset.value) || 0, appState.currentSettings.unitMode), 1, 300);
      saveSettings(appState.currentSettings);
      renderPreview();
    });
    elements2.labelMap.addEventListener("input", () => {
      appState.currentSettings.labelMapText = elements2.labelMap.value || "";
      saveSettings(appState.currentSettings);
      renderPreview();
      scheduleLabelPreviewRefresh(120);
    });
    elements2.includeShell.addEventListener("change", () => {
      appState.currentSettings.includeShell = elements2.includeShell.checked;
      saveSettings(appState.currentSettings);
      renderPreview();
    });
    elements2.invert.addEventListener("change", () => {
      appState.currentSettings.invert = elements2.invert.checked;
      saveSettings(appState.currentSettings);
      renderPreview();
    });
    elements2.reset.addEventListener("click", () => {
      appState.currentSettings = { ...DEFAULT_SETTINGS2 };
      saveSettings(appState.currentSettings);
      applySettings(appState.currentSettings);
      scheduleLabelPreviewRefresh(0);
    });
    elements2.refreshLabelPreview.addEventListener("click", () => {
      syncSettingsFromForm2();
      void refreshLabelPreview();
    });
    elements2.clearLabelOverrides.addEventListener("click", async () => {
      try {
        const header = await resolveSelectedHeader();
        if (!clearManualLabelOverrides(header.componentId)) {
          eda.sys_Message.showToastMessage("当前排针没有已保存的手改标签。", ESYS_ToastMessageType.WARNING, 3);
          return;
        }
        await refreshLabelPreview();
        eda.sys_Message.showToastMessage("已清空当前排针的手改标签。", ESYS_ToastMessageType.SUCCESS, 2);
      } catch (error) {
        const message = error instanceof Error ? error.message : String(error);
        eda.sys_Message.showToastMessage(message, ESYS_ToastMessageType.ERROR, 4);
      }
    });
    elements2.deleteGenerated.addEventListener("click", () => {
      void startDeleteGenerated();
    });
    elements2.generate.addEventListener("click", () => {
      void startPlacement();
    });
  }
  window.addEventListener("DOMContentLoaded", async () => {
    await populateFontOptions(appState.currentSettings);
    activateTab("compact");
    applySettings(appState.currentSettings);
    bindSettings();
    startLabelPreviewSelectionWatcher();
    scheduleLabelPreviewRefresh(0);
  });
})();
